# Grafana 数据源配置 - Production 环境
apiVersion: 1

datasources:
  # Prometheus 数据源
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://scaffold-ai-assistant-prometheus:9090
    isDefault: true
    editable: false  # 生产环境禁止编辑
    jsonData:
      timeInterval: "15s"
      queryTimeout: "60s"
      httpMethod: "POST"

