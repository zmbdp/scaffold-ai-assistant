# 📄 项目架构设计文档

> **文档说明**：本文档详细描述 Scaffold AI Assistant 的项目架构、核心类和数据库设计。
> 
> **核心原则**：尽可能复用现有脚手架的组件，避免重复开发，保持代码风格一致性。

---

## 7.0 脚手架复用清单

### 7.0.1 可复用的公共模块

| 模块 | 组件 | 路径 | 复用方式 | 说明 |
|-----|------|------|---------|------|
| **响应格式** | `Result<T>` | `com.zmbdp.common.domain.domain.Result` | 直接使用 | 统一响应格式，已有完整实现 |
| | `ResultCode` | `com.zmbdp.common.domain.domain.ResultCode` | 直接使用 | 响应码枚举，已有完整实现 |
| **数据库基础** | `BaseDO` | `com.zmbdp.common.domain.domain.entity.BaseDO` | 实体继承 | 实体基类，包含id字段 |
| | `MybatisPlusConfig` | `com.zmbdp.common.core.config.MybatisPlusConfig` | 自动配置 | MyBatis-Plus配置，自动生效 |
| **安全认证** | `TokenService` | `com.zmbdp.common.security.service.TokenService` | 直接注入 | Token创建、验证、刷新、删除 |
| | `JwtUtil` | `com.zmbdp.common.security.utils.JwtUtil` | 直接使用 | JWT工具类 |
| | `SecurityUtil` | `com.zmbdp.common.security.utils.SecurityUtil` | 直接使用 | 安全工具类 |
| | `GlobalExceptionHandler` | `com.zmbdp.common.security.handler.GlobalExceptionHandler` | 自动配置 | 全局异常处理，自动生效 |
| **Redis服务** | `RedisService` | `com.zmbdp.common.redis.service.RedisService` | 直接注入 | Redis操作封装，支持所有数据结构 |
| **日志管理** | `@LogAction` | `com.zmbdp.common.log.annotation.LogAction` | 直接使用 | 操作日志注解 |
| | `LogActionAspect` | `com.zmbdp.common.log.aspect.LogActionAspect` | 自动配置 | 日志切面，自动生效 |
| **限流** | `@RateLimit` | `com.zmbdp.common.ratelimit.annotation.RateLimit` | 直接使用 | 限流注解 |
| | `RateLimitAspect` | `com.zmbdp.common.ratelimit.aspect.RateLimitAspect` | 自动配置 | 限流切面，自动生效 |
| **工具类** | `JsonUtil` | `com.zmbdp.common.core.utils.JsonUtil` | 直接使用 | JSON序列化/反序列化 |
| | `FileUtil` | `com.zmbdp.common.core.utils.FileUtil` | 直接使用 | 文件操作工具 |
| | `StringUtil` | `com.zmbdp.common.core.utils.StringUtil` | 直接使用 | 字符串操作工具 |
| | `BeanCopyUtil` | `com.zmbdp.common.core.utils.BeanCopyUtil` | 直接使用 | 对象复制工具 |
| | `AESUtil` | `com.zmbdp.common.core.utils.AESUtil` | 直接使用 | AES加密工具，用于API Key加密存储 |
| | `DesensitizeUtil` | `com.zmbdp.common.core.utils.DesensitizeUtil` | 直接使用 | 数据脱敏工具，用于API Key脱敏显示 |
| | `PageUtil` | `com.zmbdp.common.core.utils.PageUtil` | 直接使用 | 分页工具 |
| | `ClientIpUtil` | `com.zmbdp.common.core.utils.ClientIpUtil` | 直接使用 | 获取客户端IP地址 |
| | `TimestampUtil` | `com.zmbdp.common.core.utils.TimestampUtil` | 直接使用 | 时间戳工具，用于日期格式转换 |
| **分页组件** | `BasePageDTO` | `com.zmbdp.common.domain.domain.dto.BasePageDTO` | 直接使用 | 分页响应DTO（含totals、totalPages、list） |
| | `BasePageVO` | `com.zmbdp.common.domain.domain.vo.BasePageVO` | 直接使用 | 分页响应VO（含totals、totalPages、list） |
| | `BasePageReqDTO` | `com.zmbdp.common.domain.domain.dto.BasePageReqDTO` | 直接使用 | 分页请求DTO（含pageNo、pageSize） |
| **幂等性** | `@Idempotent` | `com.zmbdp.common.idempotent.annotation.Idempotent` | 直接使用 | 幂等性注解，支持防重和强幂等模式 |
| | `IdempotentAspect` | `com.zmbdp.common.idempotent.aspect.IdempotentAspect` | 自动配置 | 幂等性切面，自动生效 |
| **分布式锁** | `RedissonLockService` | `com.zmbdp.common.redis.service.RedissonLockService` | 直接注入 | Redisson分布式锁 |
| | `RedissonService` | `com.zmbdp.common.redis.service.RedissonService` | 直接注入 | Redisson基础服务 |
| **雪花算法** | `SnowflakeIdService` | `com.zmbdp.common.snowflake.service.SnowflakeIdService` | 直接注入 | ID生成器 |
| **常量类** | `UserConstants` | `com.zmbdp.common.domain.constants.UserConstants` | 直接使用 | 用户常量（USER_FROM_TU_B/SYS等） |
| | `SecurityConstants` | `com.zmbdp.common.domain.constants.SecurityConstants` | 直接使用 | 安全常量 |
| | `TokenConstants` | `com.zmbdp.common.domain.constants.TokenConstants` | 直接使用 | Token常量 |
| **异常类** | `ServiceException` | `com.zmbdp.common.domain.exception.ServiceException` | 直接使用 | 业务异常类 |

### 7.0.2 可复用的业务模块

| 模块 | 组件 | 路径 | 复用方式 | 说明 |
|-----|------|------|---------|------|
| **C端用户API** | `AppUserApi` | `com.zmbdp.admin.api.appuser.feign.AppUserApi` | Feign调用 | 已包含手机号/邮箱/openId登录查询 |
| | `AppUserVO` | `com.zmbdp.admin.api.appuser.domain.vo.AppUserVO` | 直接使用 | C端用户VO |
| | `UserEditReqDTO` | `com.zmbdp.admin.api.appuser.domain.dto.UserEditReqDTO` | 直接使用 | 用户编辑DTO |
| | `AppUserDTO` | `com.zmbdp.admin.api.appuser.domain.dto.AppUserDTO` | 直接使用 | C端用户DTO |
| **文件服务API** | `FileServiceApi` | `com.zmbdp.file.api.feign.FileServiceApi` | Feign调用 | 文件上传、获取签名接口 |
| | `FileVO` | `com.zmbdp.file.api.domain.vo.FileVO` | 直接使用 | 文件上传返回VO |
| **配置管理API** | `DictionaryServiceApi` | `com.zmbdp.admin.api.config.feign.DictionaryServiceApi` | Feign调用 | 字典数据查询接口 |
| | `ArgumentServiceApi` | `com.zmbdp.admin.api.config.feign.ArgumentServiceApi` | Feign调用 | 参数配置查询接口 |
| **用户表** | `sys_user` | 数据库表 | 直接使用 | 管理端用户表（已有，identity字段标识身份，权限通过Gateway的userFrom字段控制） |
| | `app_user` | 数据库表 | 直接使用 | C端用户表（已有） |
| **系统配置表** | `sys_dictionary` | 数据库表 | 直接使用 | 字典表（已有） |
| | `sys_argument` | 数据库表 | 直接使用 | 参数配置表（已有） |

### 7.0.3 需要新增的组件

| 类型 | 组件 | 说明 |
|-----|------|------|
| **Feign接口** | `ChatApi` | RAG检索Feign接口（仅/retrieve，不包含对话） |
| | `HistoryApi` | 对话历史Feign接口 |
| | `KnowledgeApi` | 知识库管理Feign接口 |
| | `AiConfigApi` | AI配置管理Feign接口 |
| | `StatisticsApi` | 统计Feign接口 |
| | `ToolApi` | 工具管理Feign接口 |
| **Controller** | `ChatController` | RAG检索接口实现（implements ChatApi） |
| | `ChatStreamController` | 流式文本对话SSE端点（WebClient调用） |
| | `ChatWithImageStreamController` | 流式图文对话SSE端点（WebClient调用） |
| | `HistoryController` | 对话历史接口实现 |
| | `KnowledgeController` | 知识库管理接口实现 |
| | `AiConfigController` | AI配置接口实现 |
| | `StatisticsController` | 统计接口实现 |
| | `ToolController` | 工具管理接口实现 |
| **Service** | `IChatService` / `ChatServiceImpl` | AI对话核心逻辑（流式） |
| | `IModelService` / `ModelServiceImpl` | 多模型管理与切换 |
| | `IKnowledgeLoaderService` / `KnowledgeLoaderServiceImpl` | 知识加载、分块与同步 |
| | `IVectorStoreService` / `VectorStoreServiceImpl` | 向量存储与检索（Milvus） |
| | `ILuceneSearchService` / `LuceneSearchServiceImpl` | 代码搜索（Lucene倒排索引） |
| | `IChatMemoryService` / `ChatMemoryServiceImpl` | 对话记忆管理（Redis） |
| | `IStatisticsService` / `StatisticsServiceImpl` | 使用统计服务 |
| | `IHistoryService` / `HistoryServiceImpl` | 对话历史管理 |
| | `IKnowledgeService` / `KnowledgeServiceImpl` | 知识库管理（B端CRUD） |
| | `IAdminService` / `AdminServiceImpl` | B端管理服务（AI业务编排） |
| **Tool** | `ReadFileTool` | 读取文件工具 |
| | `SearchCodeTool` | 代码搜索工具 |
| | `ListDirTool` | 列出目录工具 |
| | `SearchInFileTool` | 文件内搜索工具 |
| | `NacosConfigTool` | 读取Nacos配置工具 |
| | `CompareConfigTool` | 配置对比工具 |
| | `PreDeployCheckTool` | 预部署检查工具 |
| **Entity** | `SysAiKnowledgeSource` | 知识源实体 |
| | `SysAiDocument` | 文档实体 |
| | `SysAiConversation` | 对话记录实体 |
| **Mapper** | `SysAiKnowledgeSourceMapper` | 知识源Mapper |
| | `SysAiDocumentMapper` | 文档Mapper |
| | `SysAiConversationMapper` | 对话记录Mapper |
| **Config** | `AiConfig` | AI配置类 |
| | `VectorStoreConfig` | 向量存储配置 |
| | `ToolConfig` | 工具注册配置 |
| | `LuceneConfig` | Lucene配置 |
| **DTO/VO** | 各种请求/响应对象 | 见7.3节 |
| **数据库表** | `sys_ai_knowledge_source` | 知识源表 |
| | `sys_ai_document` | 文档表 |
| | `sys_ai_conversation` | 对话记录表 |

---

## 7.1 模块划分

### 7.1.1 模块结构

遵循项目现有的 `zmbdp-{service}-api` + `zmbdp-{service}-service` 双模块结构：

```
zmbdp-chat/
├── pom.xml                              # Chat模块父POM
├── zmbdp-chat-api/                      # API模块（供其他服务Feign调用，包含Feign接口、DTO、VO）
│   ├── pom.xml
│   └── src/main/java/com/zmbdp/chat/api/
│       ├── chat/                        # C端对话相关API（供portal-service调用）
│       │   ├── feign/                   # Feign接口定义
│       │   │   └── ChatApi.java         # RAG检索接口（仅检索，不包含对话）
│       │   ├── domain/dto/ChatReqDTO.java
│       │   ├── domain/dto/ChatStreamReqDTO.java       # 流式对话请求（WebClient用，非Feign）
│       │   ├── domain/dto/ChatWithImageStreamReqDTO.java  # 流式图文对话请求
│       │   └── domain/vo/ChatVO.java
│       ├── history/                     # 对话历史相关API（供portal-service调用）
│       │   ├── feign/                   # Feign接口定义
│       │   │   └── HistoryApi.java
│       │   └── domain/vo/HistoryVO.java
│       └── common/                      # 公共枚举和常量
│           └── enums/                   # 枚举类
│               ├── DocumentType.java
│               ├── DocumentStatus.java
│               ├── ModelType.java       # 模型类型枚举
│               └── OperationType.java
└── zmbdp-chat-service/                  # Service模块（AI核心能力实现）
    ├── pom.xml
    ├── Dockerfile
    └── src/main/
        ├── java/com/zmbdp/chat/service/
        │   ├── controller/              # REST API层
        │   │   ├── ChatController.java           # RAG检索接口（实现ChatApi，仅/retrieve）
        │   │   ├── ChatStreamController.java     # 流式文本对话（SSE端点，WebClient调用）
        │   │   ├── ChatWithImageStreamController.java  # 流式图文对话（SSE端点）
        │   │   └── HistoryController.java        # 对话历史接口（实现HistoryApi）
        │   ├── service/                 # 业务逻辑层（接口I前缀，实现类Impl后缀）
        │   │   ├── IChatService.java              # 流式对话接口
        │   │   ├── ChatServiceImpl.java            # 流式对话实现
        │   │   ├── IModelService.java              # 多模型管理接口
        │   │   ├── ModelServiceImpl.java           # 多模型管理实现
        │   │   ├── IKnowledgeLoaderService.java   # 知识加载与同步接口
        │   │   ├── KnowledgeLoaderServiceImpl.java
        │   │   ├── IVectorStoreService.java        # 向量存储与检索接口
        │   │   ├── VectorStoreServiceImpl.java
        │   │   ├── ILuceneSearchService.java       # Lucene代码搜索接口
        │   │   ├── LuceneSearchServiceImpl.java
        │   │   ├── IChatMemoryService.java         # 对话记忆接口
        │   │   ├── ChatMemoryServiceImpl.java
        │   │   ├── IHistoryService.java            # 对话历史接口
        │   │   ├── HistoryServiceImpl.java
        │   │   ├── IKnowledgeService.java          # 知识库管理接口（B端CRUD）
        │   │   ├── KnowledgeServiceImpl.java
        │   │   ├── IStatisticsService.java         # 统计接口
        │   │   ├── StatisticsServiceImpl.java
        │   │   ├── IAdminService.java              # B端管理服务接口
        │   │   └── AdminServiceImpl.java
        │   ├── tool/                    # Agent工具
        │   │   ├── ReadFileTool.java
        │   │   ├── SearchCodeTool.java
        │   │   ├── ListDirTool.java
        │   │   ├── SearchInFileTool.java
        │   │   ├── NacosConfigTool.java
        │   │   ├── CompareConfigTool.java
        │   │   └── PreDeployCheckTool.java
        │   ├── domain/entity/           # 数据库实体（继承BaseDO）
        │   │   ├── SysAiKnowledgeSource.java
        │   │   ├── SysAiDocument.java
        │   │   └── SysAiConversation.java
        │   ├── domain/dto/              # 内部DTO（不对外暴露）
        │   │   ├── ChatMessage.java
        │   │   ├── DeployCheckResult.java
        │   │   └── ModelConfigDTO.java
        │   ├── mapper/                  # 数据访问层（MyBatis-Plus）
        │   │   ├── SysAiKnowledgeSourceMapper.java
        │   │   ├── SysAiDocumentMapper.java
        │   │   └── SysAiConversationMapper.java
        │   ├── config/                  # 配置类
        │   │   ├── AiConfig.java
        │   │   ├── VectorStoreConfig.java
        │   │   ├── ToolConfig.java
        │   │   └── LuceneConfig.java
        │   ├── scheduled/               # 定时任务
        │   │   ├── KnowledgeSyncScheduler.java
        │   │   └── DataCleanupScheduler.java
        │   └── ZmbdpChatServiceApplication.java  # 启动类
        └── resources/
            ├── bootstrap.yml            # 应用配置（Nacos配置）
            ├── schema.sql               # 数据库初始化脚本
            └── prompt/                  # 提示词模板
                └── system-prompt.txt
```

**接口分类说明**：

> **网关路由规则**（来自 `deploy/dev/res/sql/DEFAULT_GROUP/zmbdp-gateway-service-dev.yaml`，dev/prd 一致）：
> - `/portal/**` → zmbdp-portal-service（StripPrefix=1）
> - `/admin/**` → zmbdp-admin-service（StripPrefix=1）
> - `/file/**` → zmbdp-file-service
> - `/mstemplate/**` → zmbdp-mstemplate-service
> - 网关端口：10030
> - chat-service **不直接暴露给前端**，仅通过 Feign/WebClient 内部调用

| 接口类型 | 服务归属 | 前端请求路径（经网关） | Controller 内部路径 | 调用方式 | 说明 |
|---------|---------|---------|---------|---------|------|
| **C端接口** | zmbdp-portal-service | `/portal/chat/stream` | `/chat/stream` | 前端HTTP(SSE) | 流式文本对话 |
| **C端接口** | zmbdp-portal-service | `/portal/chat/stream/image` | `/chat/stream/image` | 前端HTTP(SSE) | 流式图文对话 |
| **C端接口** | zmbdp-portal-service | `/portal/history` | `/history` | 前端HTTP | 对话历史列表 |
| **C端接口** | zmbdp-portal-service | `/portal/history/{sessionId}` | `/history/{sessionId}` | 前端HTTP | 会话详情 |
| **Feign接口** | zmbdp-chat-service | - | `/chat/retrieve` | Feign（portal → chat） | RAG检索（同步，供portal拼接Prompt） |
| **HTTP SSE端点** | zmbdp-chat-service | - | `/chat/completions/stream` | WebClient（portal → chat） | 流式文本对话，返回Flux<String> |
| **HTTP SSE端点** | zmbdp-chat-service | - | `/chat/image/completions/stream` | WebClient（portal → chat） | 流式图文对话，返回Flux<String> |
| **Feign接口** | zmbdp-chat-service | - | `/history/**` | Feign（portal → chat） | 对话历史管理 |
| **Feign接口** | zmbdp-chat-service | - | `/knowledge/**` | Feign（admin → chat） | 知识库管理 |
| **Feign接口** | zmbdp-chat-service | - | `/ai/**` | Feign（admin → chat） | AI配置管理 |
| **Feign接口** | zmbdp-chat-service | - | `/statistics/**` | Feign（admin → chat） | 统计接口 |
| **Feign接口** | zmbdp-chat-service | - | `/tools/**` | Feign（admin → chat） | 工具管理 |
| **B端接口** | zmbdp-admin-service | `/admin/knowledge/sources` | `/knowledge/sources` | 前端HTTP | 知识库管理 |
| **B端接口** | zmbdp-admin-service | `/admin/knowledge/documents` | `/knowledge/documents` | 前端HTTP | 文档管理 |
| **B端接口** | zmbdp-admin-service | `/admin/ai/config` | `/ai/config` | 前端HTTP | AI配置管理 |
| **B端接口** | zmbdp-admin-service | `/admin/ai/models` | `/ai/models` | 前端HTTP | 模型列表 |
| **B端接口** | zmbdp-admin-service | `/admin/statistics/**` | `/statistics/**` | 前端HTTP | 统计接口 |
| **B端接口** | zmbdp-admin-service | `/admin/tools/**` | `/tools/**` | 前端HTTP | 工具管理 |

**网关路由配置**（保持现有配置不变，无需修改 `zmbdp-gateway-service-dev.yaml`）：
```yaml
# 现有路由配置已支持 AI 模块，无需新增路由
spring:
  cloud:
    gateway:
      routes:
        - id: zmbdp-portal-service
          uri: lb://zmbdp-portal-service
          predicates:
            - Path=/portal/**      # AI 的 C 端接口走 /portal/chat/** 和 /portal/history/**
          filters:
            - StripPrefix=1
        - id: zmbdp-admin-service
          uri: lb://zmbdp-admin-service
          predicates:
            - Path=/admin/**        # AI 的 B 端管理接口走 /admin/knowledge/**、/admin/ai/** 等
          filters:
            - StripPrefix=1
        # ... 其他现有路由（/file/**, /mstemplate/**）保持不变
```

**路径规则说明**：
- 前端请求格式：`http://{gateway-ip}:10030/{portal|admin}/{Controller路径}`
- Gateway 配置 `StripPrefix=1`，会去掉第一段路径前缀（`/portal` 或 `/admin`）后转发到后端
- 后端 Controller 路径不包含 `/portal` 或 `/admin` 前缀，直接使用 `@RequestMapping` 定义（如 `@RequestMapping("/chat")`）
- chat-service 不暴露给前端，仅通过 Feign/WebClient 内部调用

### 7.1.2 模块职责说明

| 模块 | 职责 | 包含内容 |
|-----|------|---------|
| **zmbdp-chat-api** | 定义AI核心能力的Feign接口和数据结构，供portal-service和admin-service调用 | DTO（请求对象）、VO（响应对象）、Feign接口定义（非流式）、枚举类 |
| **zmbdp-chat-service** | AI核心能力实现（RAG检索、Agent工具调用、对话管理），提供Feign接口（同步）和HTTP SSE端点（流式） | Controller（实现Feign接口+SSE端点）、Service（业务逻辑）、Mapper（数据访问）、Entity（数据库实体）、Config（配置类）、Tool（Agent工具） |
| **zmbdp-portal-service** | C端用户入口，提供统一对话接口，承担业务编排职责（RAG检索→Prompt拼接→流式调用） | Controller（HTTP入口）、Service（业务编排，包含WebClient调用）、Feign调用（chat-api，非流式） |
| **zmbdp-admin-service** | B端管理入口，代理AI管理接口，复用现有用户管理体系 | Controller（HTTP入口）、Service（业务编排）、Feign调用（chat-api） |

**调用关系**：

> **架构原则**：AI对话**必须使用流式输出**（WebClient + SSE），**不使用Feign**。
> Feign仅用于同步的非对话接口（RAG检索、历史记录、知识库管理等）。

**流式对话流程**（核心流程，portal-service承担业务编排）：
```
前端 → Gateway → zmbdp-portal-service /chat/stream
                    ↓
              1. 解析用户提问，获取sessionId
              2. Feign调用 → zmbdp-chat-api /chat/retrieve（RAG检索，获取context）
              3. 拼接Prompt（system prompt + context + 用户提问）
              4. WebClient调用 → zmbdp-chat-service /chat/completions/stream（SSE端点）
              5. 接收Flux<String>流数据，透传给前端
                    ↓
              返回SSE流数据
```

**管理接口流程**：
```
前端 → Gateway → zmbdp-admin-service /admin/knowledge/**
                    ↓
              Feign调用 → zmbdp-chat-api → zmbdp-chat-service
                    ↓
              返回管理结果
```

**核心设计原则**：
1. **职责单一**：chat-service只做AI相关的核心能力，提供Feign接口（同步）和HTTP SSE端点（流式）
2. **分层调用**：portal-service作为C端统一入口，负责认证、业务编排（RAG检索→Prompt拼接）和流透传；admin-service作为B端统一入口，负责权限控制和管理功能
3. **多模型扩展**：通过ModelType枚举和ModelService接口预留多模型切换能力
4. **流式支持**：流式对话使用HTTP SSE端点 + WebClient调用，避免Feign的流式限制

### 7.1.3 包命名规范

| 层级 | 规范 | 示例 |
|-----|------|------|
| API包 | `com.zmbdp.chat.api.{module}.domain.dto` | `com.zmbdp.chat.api.chat.domain.dto` |
| VO包 | `com.zmbdp.chat.api.{module}.domain.vo` | `com.zmbdp.chat.api.chat.domain.vo` |
| Controller包 | `com.zmbdp.chat.service.controller` | `com.zmbdp.chat.service.controller` |
| Service包 | `com.zmbdp.chat.service.service` | `com.zmbdp.chat.service.service` |
| Entity包 | `com.zmbdp.chat.service.domain.entity` | `com.zmbdp.chat.service.domain.entity` |
| Mapper包 | `com.zmbdp.chat.service.mapper` | `com.zmbdp.chat.service.mapper` |
| Config包 | `com.zmbdp.chat.service.config` | `com.zmbdp.chat.service.config` |
| Tool包 | `com.zmbdp.chat.service.tool` | `com.zmbdp.chat.service.tool` |

---

## 7.2 核心类详细设计

### 7.2.0 Service 调用关系图

```
┌─────────────────────────────────────────────────────────────────────┐
│                     zmbdp-portal-service                              │
│  ┌─────────────────────┐                                              │
│  │ PortalChatService   │──── WebClient ────► chat-service SSE 端点   │
│  │                     │──── Feign ────────► ChatApi.retrieveContext │
│  │ PortalHistoryService│──── Feign ────────► HistoryApi             │
│  └─────────────────────┘                                              │
└─────────────────────────────────────────────────────────────────────┘
                                        │ Feign
                                        ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     zmbdp-admin-service                               │
│  ┌─────────────────────┐  ┌─────────────────────┐                    │
│  │ KnowledgeController │  │ AiConfigController  │                    │
│  │ ToolController      │  │ StatisticsController│                    │
│  └────────┬────────────┘  └────────┬────────────┘                    │
│           │ Feign                  │ Feign                           │
└───────────┼────────────────────────┼────────────────────────────────┘
            │                        │
            ▼                        ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     zmbdp-chat-service                                │
│                                                                       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐        │
│  │ ChatStream   │    │ Chat         │    │ History          │        │
│  │ Controller   │    │ Controller   │    │ Controller       │        │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────────┘        │
│         │                   │                   │                    │
│         ▼                   ▼                   ▼                    │
│  ┌──────────────────────────────────────────────────────────┐        │
│  │              IChatService / ChatServiceImpl              │        │
│  │  ├─ IModelService (获取 ChatClient/EmbeddingModel)       │        │
│  │  ├─ IChatMemoryService (Redis 对话记忆)                  │        │
│  │  ├─ IHistoryService (MySQL 对话记录持久化)                │        │
│  │  ├─ IVectorStoreService (RAG 检索)                       │        │
│  │  ├─ ToolConfig (Agent 工具列表)                          │        │
│  │  └─ SysAiConfigMapper (AI 配置)                          │        │
│  └──────────────────────────────────────────────────────────┘        │
│                                                                       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐        │
│  │IModelService │    │IVectorStore  │    │IChatMemory      │        │
│  │              │    │Service       │    │Service          │        │
│  │ ChatClient池 │    │ Milvus 客户端 │    │ RedisService    │        │
│  │ EmbeddingModel│   │              │    │                 │        │
│  └──────────────┘    └──────────────┘    └──────────────────┘        │
│                                                                       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐        │
│  │IKnowledge    │    │IKnowledge    │    │ILuceneSearch    │        │
│  │Service       │───►│LoaderService │───►│Service          │        │
│  │ (B端 CRUD)   │    │ (同步/分块)  │    │ (代码搜索)       │        │
│  └──────┬───────┘    └──────┬───────┘    └──────────────────┘        │
│         │                   │                                         │
│         ▼                   ▼                                         │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐        │
│  │IAdminService │    │IStatistics   │    │IHistoryService   │        │
│  │(B端 AI 业务) │    │Service       │    │(对话历史管理)     │        │
│  └──────────────┘    └──────────────┘    └──────────────────┘        │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────┐        │
│  │                    Agent 工具                              │        │
│  │  ReadFileTool │ SearchCodeTool │ ListDirTool              │        │
│  │  SearchInFileTool │ NacosConfigTool │ CompareConfigTool   │        │
│  │  PreDeployCheckTool                                       │        │
│  │  ├─ ReadFileTool → 路径白名单 + Java NIO                  │        │
│  │  ├─ SearchCodeTool → ILuceneSearchService                 │        │
│  │  └─ NacosConfigTool → Nacos 配置中心 API                  │        │
│  └──────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────┘
```

**Service 依赖关系总结**：

| Service | 依赖的其他 Service | 依赖的外部组件 |
|---------|------------------|-------------|
| IChatService | IModelService、IChatMemoryService、IHistoryService、IVectorStoreService、ToolConfig | Spring AI ChatClient |
| IModelService | 无（自己管理 ChatClient 池） | DashScope API（通义千问） |
| IKnowledgeLoaderService | IVectorStoreService、ILuceneSearchService、IModelService | RedissonLockService（分布式锁） |
| IVectorStoreService | IModelService（获取 EmbeddingModel） | MilvusClient |
| ILuceneSearchService | 无 | Lucene IndexWriter/IndexReader |
| INacosConfigService | 无 | Nacos OpenAPI（NacosRestTemplate/WebClient） |
| IChatMemoryService | 无 | RedisService |
| IHistoryService | IChatMemoryService | SysAiConversationMapper |
| IKnowledgeService | IKnowledgeLoaderService、IVectorStoreService | SysAiKnowledgeSourceMapper、SysAiDocumentMapper |
| IStatisticsService | 无 | SysAiConversationMapper、RedisService（缓存） |
| IAdminService | IKnowledgeService、IKnowledgeLoaderService、IModelService | AESUtil、DesensitizeUtil |
| ToolConfig | SysAiToolConfigMapper | Spring 自动注入所有 @Component 工具 |

---

### 7.2.1 ChatApi（Feign接口，放在-api模块）

**类名**：`ChatApi`

**职责**：RAG检索Feign接口定义（同步），供zmbdp-portal-service调用

> **重要**：ChatApi **仅提供RAG检索接口**，不提供对话接口。
> AI对话必须使用流式输出（WebClient + SSE），不使用Feign。
> 详见 7.2.3 ChatStreamController 和 7.2.4 ChatWithImageStreamController。

**路径**：`com.zmbdp.chat.api.chat.feign.ChatApi`

**注解**：`@FeignClient(contextId = "chatApi", name = "zmbdp-chat-service", path = "/chat")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| POST | `/retrieve` | `retrieveContext()` | RAG检索，获取相关文档上下文 |

**方法详细设计**：

**retrieveContext()**：
```java
@PostMapping("/retrieve")
Result<List<DocumentVO>> retrieveContext(@RequestBody RetrieveReqDTO request);
```
- **参数**：`RetrieveReqDTO`（包含question、topK）
- **返回值**：`Result<List<DocumentVO>>`（检索到的文档列表）
- **说明**：仅执行RAG检索，返回相关文档上下文，供portal-service拼接Prompt使用

---

### 7.2.2 ~~ChatWithImageApi~~（已删除）

> **已删除**：AI对话不使用Feign，图文对话通过流式SSE端点实现（见 7.2.4 ChatWithImageStreamController）。

---

### 7.2.3 ChatStreamController（HTTP SSE端点，放在-service模块）

**类名**：`ChatStreamController`

**职责**：文本对话流式端点，直接暴露HTTP接口，供portal-service通过WebClient调用

**路径**：`com.zmbdp.chat.service.controller.ChatStreamController`

**注解**：`@RestController`, `@RequestMapping("/chat")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| POST | `/completions/stream` | `streamChat()` | 流式文本对话，返回Flux<String> |

**方法详细设计**：

**streamChat()**：
```java
@PostMapping(value = "/completions/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
Flux<String> streamChat(@RequestBody ChatStreamReqDTO request);
```
- **参数**：`ChatStreamReqDTO`（包含message、sessionId、prompt、model、temperature）
- **返回值**：`Flux<String>`（SSE流式响应，每帧为JSON格式的token）
- **说明**：处理流式文本对话请求，直接返回Flux流

**SSE响应格式**：
```
data: {"chunk": "三级缓存架构包含", "done": false}

data: {"chunk": "布隆过滤器、Caffeine本地缓存", "done": false}

data: {"chunk": "", "done": true, "sessionId": "abc123", "sources": [...]}
```

---

### 7.2.4 ChatWithImageStreamController（HTTP SSE端点，放在-service模块）

**类名**：`ChatWithImageStreamController`

**职责**：图文对话流式端点，直接暴露HTTP接口，供portal-service通过WebClient调用

**路径**：`com.zmbdp.chat.service.controller.ChatWithImageStreamController`

**注解**：`@RestController`, `@RequestMapping("/chat/image")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| POST | `/completions/stream` | `streamChatWithImage()` | 流式图文对话，返回Flux<String> |

**方法详细设计**：

**streamChatWithImage()**：
```java
@PostMapping(value = "/completions/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
Flux<String> streamChatWithImage(@RequestBody ChatWithImageStreamReqDTO request);
```
- **参数**：`ChatWithImageStreamReqDTO`（包含message、images、sessionId、prompt、model）
- **返回值**：`Flux<String>`（SSE流式响应）
- **说明**：处理流式图文对话请求，直接返回Flux流

---

### 7.2.5 ChatController（实现Feign接口，放在-service模块）

**类名**：`ChatController`

**职责**：文本对话接口实现

**路径**：`com.zmbdp.chat.service.controller.ChatController`

**注解**：`@RestController`, `@RequestMapping("/chat")`

**实现接口**：`ChatApi`

---

### 7.2.6 ~~ChatWithImageController~~（已删除）

> **已删除**：ChatWithImageApi 已删除，图文对话通过 `ChatWithImageStreamController`（SSE端点）实现，不使用Feign。

---

### 7.2.7 HistoryApi（Feign接口，放在-api模块）

**类名**：`HistoryApi`

**职责**：对话历史Feign接口定义，供zmbdp-portal-service调用

**路径**：`com.zmbdp.chat.api.history.feign.HistoryApi`

**注解**：`@FeignClient(contextId = "historyApi", name = "zmbdp-chat-service", path = "/history")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| GET | `/` | `getHistory()` | 获取对话历史列表 |
| GET | `/{sessionId}` | `getSessionHistory()` | 获取会话详情 |
| DELETE | `/{sessionId}` | `deleteSession()` | 删除会话 |
| POST | `/{sessionId}/continue` | `continueChat()` | 继续对话 |

---

### 7.2.8 HistoryController（实现Feign接口，放在-service模块）

**类名**：`HistoryController`

**职责**：对话历史接口实现

**路径**：`com.zmbdp.chat.service.controller.HistoryController`

**注解**：`@RestController`, `@RequestMapping("/history")`

**实现接口**：`HistoryApi`

---

### 7.2.9 KnowledgeApi（Feign接口，放在-api模块）

**类名**：`KnowledgeApi`

**职责**：知识库管理Feign接口定义，供zmbdp-admin-service调用

**路径**：`com.zmbdp.chat.api.knowledge.feign.KnowledgeApi`

**注解**：`@FeignClient(contextId = "knowledgeApi", name = "zmbdp-chat-service", path = "/knowledge")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 参数 | 返回值 | 说明 |
|---------|------|-------|------|-------|------|
| GET | `/sources` | `listSources()` | `KnowledgeSourceListReqDTO`（继承BasePageReqDTO，含pageNo、pageSize、type） | `Result<BasePageVO<KnowledgeSourceVO>>` | 获取知识源列表（分页） |
| POST | `/sources` | `createSource()` | `KnowledgeSourceReqDTO` | `Result<KnowledgeSourceVO>` | 新增知识源 |
| PUT | `/sources/{id}` | `updateSource()` | `@PathVariable Long id, KnowledgeSourceReqDTO` | `Result<Void>` | 更新知识源 |
| DELETE | `/sources/{id}` | `deleteSource()` | `@PathVariable Long id` | `Result<Void>` | 删除知识源（软删除） |
| POST | `/sync` | `syncKnowledge()` | `SyncReqDTO` | `Result<SyncResultVO>` | 触发知识同步 |
| GET | `/documents` | `listDocuments()` | `pageNo, pageSize, sourceId(可选), status(可选)` | `Result<BasePageVO<KnowledgeDocumentVO>>` | 获取文档列表（分页） |
| POST | `/documents/upload` | `uploadDocument()` | `@RequestParam Long knowledgeSourceId, MultipartFile file` | `Result<KnowledgeDocumentVO>` | 上传文档 |
| GET | `/documents/{id}` | `getDocument()` | `@PathVariable Long id` | `Result<KnowledgeDocumentVO>` | 获取文档详情（含完整内容） |
| DELETE | `/documents/{id}` | `deleteDocument()` | `@PathVariable Long id` | `Result<Void>` | 删除文档 |

---

### 7.2.10 AiConfigApi（Feign接口，放在-api模块）

**类名**：`AiConfigApi`

**职责**：AI配置Feign接口定义，供zmbdp-admin-service调用

**路径**：`com.zmbdp.chat.api.config.feign.AiConfigApi`

**注解**：`@FeignClient(contextId = "aiConfigApi", name = "zmbdp-chat-service", path = "/ai")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| GET | `/config` | `getConfig()` | 获取AI配置 |
| PUT | `/config` | `updateConfig()` | 更新AI配置 |
| POST | `/test` | `testConnection()` | 测试AI连接 |

---

### 7.2.11 StatisticsApi（Feign接口，放在-api模块）

**类名**：`StatisticsApi`

**职责**：统计Feign接口定义，供zmbdp-admin-service调用

**路径**：`com.zmbdp.chat.api.statistics.feign.StatisticsApi`

**注解**：`@FeignClient(contextId = "statisticsApi", name = "zmbdp-chat-service", path = "/statistics")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| GET | `/conversations` | `getConversationStats()` | 获取对话统计 |
| GET | `/questions` | `getTopQuestions()` | 获取热门问题 |
| GET | `/users` | `getUserStats()` | 获取用户统计 |
| GET | `/tools` | `getToolUsageStats()` | 获取工具使用统计 |
| GET | `/ai-metrics` | `getAiMetrics()` | 获取AI调用指标 |

---

### 7.2.12 ToolApi（Feign接口，放在-api模块）

**类名**：`ToolApi`

**职责**：工具管理Feign接口定义，供zmbdp-admin-service调用

**路径**：`com.zmbdp.chat.api.tool.feign.ToolApi`

**注解**：`@FeignClient(contextId = "toolApi", name = "zmbdp-chat-service", path = "/tools")`

**API接口**：

| HTTP方法 | 路径 | 方法名 | 说明 |
|---------|------|-------|------|
| GET | `/` | `listTools()` | 获取工具列表 |
| PUT | `/{name}` | `updateTool()` | 更新工具配置 |
| POST | `/{name}/test` | `testTool()` | 测试工具 |

---

### 7.2.13 IChatService / ChatServiceImpl

**类名**：`IChatService`（接口） / `ChatServiceImpl`（实现类）

**职责**：AI对话核心逻辑，负责流式调用大模型、工具调用编排、对话记忆管理和对话记录持久化

**路径**：
- 接口：`com.zmbdp.chat.service.service.IChatService`
- 实现：`com.zmbdp.chat.service.service.ChatServiceImpl`

> **重要**：ChatService 仅负责**流式对话**（返回 `Flux<String>`），不提供同步对话方法。
> 同步的 RAG 检索由 `ChatController.retrieveContext()` 直接委托给 `VectorStoreService` 完成。
> Prompt 拼接由 `portal-service` 的 `PortalChatService` 完成，chat-service 接收已拼接好的 `prompt`。

**依赖注入**：
- `IModelService` - 获取 ChatClient 和 EmbeddingModel
- `IChatMemoryService` - 对话记忆管理（Redis）
- `IHistoryService` - 对话记录持久化（MySQL）
- `IVectorStoreService` - 向量检索（RAG 检索时使用）
- `ToolConfig` - 获取已启用的 Agent 工具列表
- `SysAiConfigMapper` - 读取 AI 配置（default_model、enable_tools 等）
- `SnowflakeIdService` - 生成对话记录 ID

**核心方法**：

**streamChat()**：
```java
Flux<String> streamChat(ChatStreamReqDTO request);
```
- **参数**：`ChatStreamReqDTO` - 包含已拼接好的完整 prompt、sessionId、model、temperature
- **返回值**：`Flux<String>` - SSE 流式响应，每帧为 JSON 格式
- **说明**：流式文本对话，由 ChatStreamController 调用

**streamChatWithImage()**：
```java
Flux<String> streamChatWithImage(ChatWithImageStreamReqDTO request);
```
- **参数**：`ChatWithImageStreamReqDTO` - 包含 prompt、images、sessionId、model
- **返回值**：`Flux<String>` - SSE 流式响应
- **说明**：流式图文对话，自动选择支持图片的模型

**retrieveContext()**：
```java
List<DocumentVO> retrieveContext(RetrieveReqDTO request);
```
- **参数**：`RetrieveReqDTO` - 包含 question、topK、filters
- **返回值**：`List<DocumentVO>` - 检索到的文档列表
- **说明**：执行 RAG 检索（供 ChatController.retrieveContext() 调用，不涉及对话）

**streamChat() 方法执行流程**：
```
输入：ChatStreamReqDTO（已包含 portal-service 拼接好的完整 prompt）
  │
  ├─ Step 1: 解析参数，确定使用的模型
  │   ├─ 如果 request.model 为空，从 sys_ai_config 读取 default_model
  │   └─ 通过 ModelService.getChatClient(model) 获取 ChatClient
  │
  ├─ Step 2: 获取对话历史（如果 sessionId 不为空）
  │   └─ ChatMemoryService.getHistory(sessionId) → List<ChatMessage>
  │
  ├─ Step 3: 构建消息列表
  │   ├─ 将历史消息添加到上下文
  │   └─ 使用 request.prompt 作为当前用户消息
  │
  ├─ Step 4: 判断是否需要工具调用（通过 ChatClient 的函数调用能力）
  │   ├─ 如果 enable_tools=true 且模型支持 FUNCTION_CALL
  │   │   └─ 配置工具列表（从 ToolConfig 获取已启用的工具）
  │   └─ 否则跳过工具调用
  │
  ├─ Step 5: 调用 ChatClient.stream() 流式生成
  │   ├─ chatClient.prompt(messages).stream().content()
  │   └─ 返回 Flux<String>（每个元素为一个 token 片段）
  │
  ├─ Step 6: 流式输出完成后，异步执行后处理
  │   ├─ 拼接完整回答内容
  │   ├─ 保存对话历史到 Redis：ChatMemoryService.addMessage(sessionId, ...)
  │   ├─ 记录对话到 MySQL：sys_ai_conversation 表（含 model、response_time、token_usage）
  │   └─ 如果使用了工具，记录 tool_usage 信息
  │
  └─ 返回 Flux<String>（SSE 格式，每帧包含 chunk、done 等字段）
```

**SSE 响应帧格式**：
```json
// 内容帧
{"chunk": "三级缓存架构包含", "done": false}

// 工具调用帧（如果触发了工具调用）
{"toolCall": {"name": "ReadFileTool", "args": {"filePath": "/path/to/file"}}, "done": false}

// 结束帧
{"chunk": "", "done": true, "sessionId": "abc123", "sources": [...], "model": "qwen-max"}
```

**错误处理**：
- 模型调用失败：返回错误帧 `{"error": "AI服务连接失败", "code": 500023, "done": true}`
- 工具调用失败：记录错误但继续生成回答（不中断流）
- 超时（60秒无响应）：返回超时帧 `{"error": "AI服务响应超时", "code": 500024, "done": true}`

---

### 7.2.14 IModelService / ModelServiceImpl

**类名**：`IModelService`（接口） / `ModelServiceImpl`（实现类）

**职责**：多模型管理与切换，为不同场景选择合适的模型；维护 ChatClient 和 EmbeddingModel 实例池

**路径**：
- 接口：`com.zmbdp.chat.service.service.IModelService`
- 实现：`com.zmbdp.chat.service.service.ModelServiceImpl`

**核心方法**：

**selectModel()**：
```java
ModelConfig selectModel(ChatRequest request)
```
- **参数**：`ChatRequest` - 包含请求类型（文本/图文）、用户指定模型名
- **返回值**：`ModelConfig` - 选中的模型配置
- **说明**：根据请求类型和用户权限选择合适的模型

**getChatClient()**：
```java
ChatClient getChatClient(String modelName)
```
- **参数**：`modelName` - 模型名称（如 `qwen-max`）
- **返回值**：`ChatClient` - 对应的 ChatClient 实例
- **说明**：从 ChatClient 实例池中获取指定模型的客户端

**getDefaultChatClient()**：
```java
ChatClient getDefaultChatClient()
```
- **参数**：无
- **返回值**：`ChatClient` - 默认文本对话 ChatClient
- **说明**：获取配置中 `default=true` 的文本模型 ChatClient

**getEmbeddingModel()**：
```java
EmbeddingModel getEmbeddingModel()
```
- **参数**：无
- **返回值**：`EmbeddingModel` - 当前配置的 Embedding 模型
- **说明**：获取用于向量生成的 Embedding 模型（text-embedding-v1）

**listModels()**：
```java
List<ModelVO> listModels()
```
- **参数**：无
- **返回值**：`List<ModelVO>` - 所有可用模型列表（含 capabilities、isDefault 字段）
- **说明**：获取系统支持的所有模型配置，供 admin-service 查询

**执行流程**：
```
1. @PostConstruct 启动时初始化：
   ├─ 从 Nacos 读取 spring.ai.models 配置列表
   ├─ 为每个 enabled=true 的模型创建 ChatClient 实例
   └─ 缓存到 Map<String, ChatClient> chatClientPool
2. selectModel(request) 调用时：
   ├─ 若 request.model 非空，从 chatClientPool 获取对应实例
   ├─ 若 request.model 为空，根据请求类型返回默认模型
   │   ├─ 文本请求 → 默认文本模型（default=true）
   │   └─ 图文请求 → 默认图文模型（capabilities 含 IMAGE）
   └─ 若指定模型不存在或未启用，抛 ServiceException
3. getEmbeddingModel() 直接返回单例 EmbeddingModel
4. 运行时切换默认模型：更新 Nacos 配置 + 刷新 chatClientPool
```

**模型配置示例**（位于 `zmbdp-chat-service-${env}.yaml`）：
```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}    # 从 Nacos 读取，加密存储
      chat:
        model: qwen-max
        options:
          temperature: 0.7
          max-tokens: 4096
      embedding:
        model: text-embedding-v1
    models:
      - type: TEXT_ONLY
        name: qwen-max
        provider: dashscope
        enabled: true
        default: true
        capabilities: [TEXT]
      - type: TEXT_AND_IMAGE
        name: qwen-vl-max
        provider: dashscope
        enabled: true
        default: false
        capabilities: [TEXT, IMAGE]
```

---

### 7.2.15 IKnowledgeLoaderService / KnowledgeLoaderServiceImpl

**类名**：`IKnowledgeLoaderService`（接口） / `KnowledgeLoaderServiceImpl`（实现类）

**职责**：知识加载、分块处理、Lucene索引构建；执行知识同步（增量/全量）

**路径**：
- 接口：`com.zmbdp.chat.service.service.IKnowledgeLoaderService`
- 实现：`com.zmbdp.chat.service.service.KnowledgeLoaderServiceImpl`

**依赖注入**：
- `SysAiKnowledgeSourceMapper` - 知识源 Mapper
- `SysAiDocumentMapper` - 文档 Mapper
- `IVectorStoreService` - 向量存储服务
- `ILuceneSearchService` - Lucene 索引服务
- `IModelService` - 用于获取 EmbeddingModel
- `RedissonLockService` - 分布式锁（防止并发同步）

**核心方法**：

**loadAllKnowledge()**：
```java
void loadAllKnowledge()
```
- **参数**：无
- **返回值**：无
- **说明**：加载所有知识源（文档、JavaDoc、配置文件），同步到 MySQL 和 Milvus，并构建 Lucene 索引

**loadDocs()**：
```java
List<Document> loadDocs()
```
- **参数**：无
- **返回值**：`List<Document>` - 加载的文档列表
- **说明**：加载 docs/*.md 文档

**loadJavaDoc()**：
```java
List<Document> loadJavaDoc()
```
- **参数**：无
- **返回值**：`List<Document>` - 加载的 JavaDoc 文档列表
- **说明**：加载 JavaDoc HTML 文档

**loadConfigs()**：
```java
List<Document> loadConfigs()
```
- **参数**：无
- **返回值**：`List<Document>` - 加载的配置文件列表
- **说明**：加载 Nacos 配置和部署配置文件

**chunkDocument()**：
```java
List<Document> chunkDocument(Document document)
```
- **参数**：`document` - 原始文档（含 content、metadata）
- **返回值**：`List<Document>` - 分块后的文档列表（每个分块含 chunkIndex）
- **说明**：根据文档类型进行分块处理（详见 06-RAG流程设计.md 6.1节）

**buildLuceneIndex()**：
```java
void buildLuceneIndex(List<Document> documents)
```
- **参数**：`documents` - 文档列表
- **返回值**：无
- **说明**：构建 Lucene 倒排索引，用于代码搜索

**syncKnowledge()**：
```java
SyncResultVO syncKnowledge(boolean force)
```
- **参数**：`force` - 是否强制同步（跳过哈希检查）
- **返回值**：`SyncResultVO` - 同步结果（totalDocuments、updatedDocuments、deletedDocuments、skippedDocuments、failedDocuments、duration）
- **说明**：执行知识同步，处理新增、更新和删除的文档

**syncKnowledge执行流程**：
```
1. 获取分布式锁 redissonLockService.lock("knowledge:sync")
   └─ 防止并发同步冲突（同一时刻只允许一个同步任务）
2. 从 sys_ai_knowledge_source 表查询所有 enabled=1 的知识源
3. 遍历每个知识源目录，收集当前所有文件路径
4. 从 sys_ai_document 表查询已有文档列表（按知识源分组）
5. 对比文件路径，识别三种状态：
   ├─ 新增文件：当前目录有，MySQL 没有
   ├─ 更新文件：当前目录有，MySQL 有，但 SHA-256 哈希不同
   └─ 删除文件：当前目录没有，MySQL 有
6. 处理新增文件：
   ├─ 读取文件内容，计算 SHA-256 哈希
   ├─ 调用 chunkDocument() 分块
   ├─ 插入 sys_ai_document 表，获取文档ID
   ├─ 调用 vectorStoreService.addDocuments() 写入 Milvus
   └─ 调用 luceneSearchService.updateIndex() 更新 Lucene 索引
7. 处理更新文件：
   ├─ 读取文件内容，计算 SHA-256 哈希
   ├─ 更新 sys_ai_document 表，version+1
   ├─ 调用 vectorStoreService.deleteByDocumentId() 删除旧分块
   ├─ 调用 chunkDocument() 重新分块
   ├─ 调用 vectorStoreService.addDocuments() 写入新分块
   └─ 调用 luceneSearchService.updateIndex() 更新索引
8. 处理删除文件：
   ├─ 调用 vectorStoreService.deleteByDocumentId() 删除 Milvus 分块
   └─ 更新 sys_ai_document 表 status='DELETED'（软删除）
9. 若 force=true：跳过哈希检查，所有文件视为更新文件重新处理
10. 释放分布式锁
11. 记录操作日志到 sys_ai_operation_log 表
12. 返回 SyncResultVO（含各项统计数量）
```

---

### 7.2.16 IVectorStoreService / VectorStoreServiceImpl

**类名**：`IVectorStoreService`（接口） / `VectorStoreServiceImpl`（实现类）

**职责**：向量存储与检索，基于 Milvus 实现；封装 Embedding 生成和向量 CRUD

**路径**：
- 接口：`com.zmbdp.chat.service.service.IVectorStoreService`
- 实现：`com.zmbdp.chat.service.service.VectorStoreServiceImpl`

**依赖注入**：
- `MilvusClient` - Milvus 客户端
- `IModelService` - 用于获取 EmbeddingModel
- `SnowflakeIdService` - 生成向量记录ID

**Milvus 集合**：`scaffold_knowledge`（结构详见 06-RAG流程设计.md 6.2.2节）

**核心方法**：

**addDocuments()**：
```java
void addDocuments(List<Document> documents)
```
- **参数**：`documents` - 文档列表（已分块，每个 Document 含 content、metadata）
- **返回值**：无
- **说明**：将文档转换为向量并写入 Milvus

**search()**：
```java
List<DocumentVO> search(String query, int topK)
```
- **参数**：`query` - 查询文本，`topK` - 返回数量（1-20）
- **返回值**：`List<DocumentVO>` - 检索到的文档列表（含 documentId、score、chunkIndex）
- **说明**：执行向量相似性检索

**searchWithFilter()**：
```java
List<DocumentVO> searchWithFilter(String query, int topK, Map<String, String> filters)
```
- **参数**：`query` - 查询文本，`topK` - 返回数量，`filters` - 元数据过滤条件（如 module、source_type）
- **返回值**：`List<DocumentVO>` - 检索到的文档列表
- **说明**：带元数据过滤的向量检索

**deleteByFilter()**：
```java
void deleteByFilter(Map<String, String> filters)
```
- **参数**：`filters` - 删除条件
- **返回值**：无
- **说明**：按条件删除向量数据

**deleteByDocumentId()**：
```java
void deleteByDocumentId(Long documentId)
```
- **参数**：`documentId` - 文档ID
- **返回值**：无
- **说明**：删除指定文档的所有向量分块（用于增量更新和删除）

**getDocumentCount()**：
```java
long getDocumentCount()
```
- **参数**：无
- **返回值**：`long` - Milvus 中分块总数
- **说明**：获取知识库文档分块数量

**initCollection()**：
```java
void initCollection()
```
- **参数**：无
- **返回值**：无
- **说明**：幂等初始化 Milvus 集合和索引（集合不存在则创建，索引不存在则创建）

**addDocuments执行流程**：
```
1. 遍历传入的 documents 列表
2. 对每个 document：
   ├─ 调用 modelService.getEmbeddingModel().embed(content) 生成向量
   │   └─ 返回 float[]（维度1536，text-embedding-v1）
   ├─ 构建 Milvus InsertReq：
   │   ├─ id: snowflakeIdService.nextId()
   │   ├─ content: 分块内容
   │   ├─ document_id: 关联 MySQL 文档ID
   │   ├─ chunk_index: 分块序号
   │   ├─ metadata: 元数据 Map（source_type、source_path、module、category、title、create_time）
   │   ├─ embedding_model: "text-embedding-v1"
   │   └─ vector: float[] 向量
   └─ 执行 milvusClient.insert()
3. 返回（不抛异常即视为成功）
```

**search执行流程**：
```
1. 调用 modelService.getEmbeddingModel().embed(query) 生成查询向量
2. 构建 Milvus SearchReq：
   ├─ collection_name: "scaffold_knowledge"
   ├─ vector field: 查询向量
   ├─ top_k: 请求的 topK（限制1-20）
   ├─ metric_type: IP（内积）
   └─ params: {"ef": 100}（HNSW查询参数）
3. 执行 milvusClient.search()
4. 解析返回结果：
   ├─ 提取 id、content、document_id、chunk_index、metadata
   ├─ 计算 score（相似度分数）
   └─ 构建 DocumentVO 列表
5. 按 score 降序排序返回
```

---

### 7.2.17 ILuceneSearchService / LuceneSearchServiceImpl

**类名**：`ILuceneSearchService`（接口） / `LuceneSearchServiceImpl`（实现类）

**职责**：基于 Lucene 倒排索引的代码搜索服务，提升 SearchCodeTool 的搜索效率

**路径**：
- 接口：`com.zmbdp.chat.service.service.ILuceneSearchService`
- 实现：`com.zmbdp.chat.service.service.LuceneSearchServiceImpl`

**依赖注入**：
- `LuceneConfig` - Lucene 配置（索引目录、Analyzer 等）

**索引结构**（字段定义详见 05-Agent工具设计.md 5.2.2节）：
- `filePath`（存储字段）- 文件路径
- `className`（分词索引）- 类名
- `methodName`（分词索引）- 方法名
- `content`（分词索引）- 文件内容（全文搜索）
- `lineNumber`（存储字段）- 行号

**核心方法**：

**search()**：
```java
List<SearchResult> search(String keyword, int limit)
```
- **参数**：`keyword` - 搜索关键词（类名、方法名等），`limit` - 返回数量（默认10）
- **返回值**：`List<SearchResult>` - 搜索结果列表（含 filePath、className、methodName、lineNumber、context、score）
- **说明**：在代码库中搜索类名、方法名等

**searchInFile()**：
```java
List<SearchResult> searchInFile(String filePath, String keyword, int limit)
```
- **参数**：`filePath` - 文件路径，`keyword` - 搜索关键词，`limit` - 返回数量
- **返回值**：`List<SearchResult>` - 文件内搜索结果
- **说明**：在指定文件中搜索关键词

**buildIndex()**：
```java
void buildIndex(String basePath)
```
- **参数**：`basePath` - 代码库基础路径
- **返回值**：无
- **说明**：构建 Lucene 索引（全量）

**updateIndex()**：
```java
void updateIndex(String filePath, String content)
```
- **参数**：`filePath` - 文件路径，`content` - 文件内容
- **返回值**：无
- **说明**：更新指定文件的索引（增量）

**deleteFromIndex()**：
```java
void deleteFromIndex(String filePath)
```
- **参数**：`filePath` - 文件路径
- **返回值**：无
- **说明**：从索引中删除指定文件

**search执行流程**：
```
1. 使用 StandardAnalyzer 分词关键词
2. 构建 BooleanQuery：
   ├─ 应包含 className 字段的 TermQuery
   ├─ 应包含 methodName 字段的 TermQuery
   └─ 应包含 content 字段的 PhraseQuery（全文匹配）
3. 打开 IndexReader（只读模式）
4. 创建 IndexSearcher
5. 执行 searcher.search(query, limit)
6. 遍历 TopDocs：
   ├─ 获取 document.get("filePath")
   ├─ 获取 document.get("className")
   ├─ 获取 document.get("methodName")
   ├─ 获取 document.get("lineNumber")
   ├─ 提取匹配行上下文 context
   └─ 计算 score（相关性分数）
7. 关闭 IndexReader
8. 返回 List<SearchResult>
```

**buildIndex执行流程**：
```
1. 创建 IndexWriter（配置 analyzer 和索引目录）
2. 遍历 basePath 下所有 .java 文件：
   ├─ 读取文件内容
   ├─ 解析类名（class/interface 声明）
   ├─ 解析方法名（public/private/protected 方法声明）
   └─ 创建 Lucene Document 并添加到 IndexWriter
3. 调用 indexWriter.commit()
4. 关闭 IndexWriter
5. 缓存索引到本地磁盘（路径见 LuceneConfig）
```

---

### 7.2.17.1 INacosConfigService / NacosConfigServiceImpl

**类名**：`INacosConfigService`（接口） / `NacosConfigServiceImpl`（实现类）

**职责**：封装 Nacos 配置中心的 HTTP API 调用，支持按 dataId/group/namespace 查询配置内容

**路径**：
- 接口：`com.zmbdp.chat.service.service.INacosConfigService`
- 实现：`com.zmbdp.chat.service.service.NacosConfigServiceImpl`

**依赖注入**：
- `NacosRestTemplate` 或 `WebClient` - 用于调用 Nacos OpenAPI
- `@Value("${nacos.config.server-addr}")` - Nacos 服务地址

**核心方法**：

**getConfig()**：
```java
String getConfig(String dataId, String group, String namespace)
```
- **参数**：`dataId` - 配置ID，`group` - 分组（默认 DEFAULT_GROUP），`namespace` - 命名空间
- **返回值**：配置内容字符串
- **说明**：调用 Nacos OpenAPI `GET /nacos/v1/cs/configs?dataId={dataId}&group={group}&tenant={namespace}` 获取配置

**listConfigs()**：
```java
List<ConfigItem> listConfigs(String group, String namespace)
```
- **参数**：`group` - 分组，`namespace` - 命名空间
- **返回值**：配置项列表
- **说明**：列出指定分组下的所有配置项

**getConfig执行流程**：
```
1. 构建 Nacos OpenAPI URL：http://{server-addr}/nacos/v1/cs/configs
2. 设置请求参数：dataId、group、tenant(namespace)
3. 发送 HTTP GET 请求（通过 NacosRestTemplate 或 WebClient）
4. 解析响应，返回配置内容字符串
5. 若配置不存在，返回 null
```

> **说明**：该 Service 被 NacosConfigTool、CompareConfigTool、PreDeployCheckTool 三个工具调用，用于查询 Nacos 配置中心的内容。

---

### 7.2.18 IChatMemoryService / ChatMemoryServiceImpl

**类名**：`IChatMemoryService`（接口） / `ChatMemoryServiceImpl`（实现类）

**职责**：对话记忆管理，基于 Redis 存储；实现 Spring AI 的 `ChatMemory` 接口以支持流式对话的上下文记忆

**路径**：
- 接口：`com.zmbdp.chat.service.service.IChatMemoryService`
- 实现：`com.zmbdp.chat.service.service.ChatMemoryServiceImpl`

**依赖注入**：
- `RedisService` - Redis 操作封装
- `JsonUtil` - 序列化工具

**Redis 存储结构**：
```
Key: chat:history:{sessionId}
Type: List
Value: JSON 序列化的 ChatMessage 列表
TTL: 7天（604800秒）
```

**核心方法**：

**getHistory()**：
```java
List<ChatMessage> getHistory(String sessionId)
```
- **参数**：`sessionId` - 会话ID
- **返回值**：`List<ChatMessage>` - 对话历史列表（按时间正序）
- **说明**：获取会话的对话历史

**addMessage()**：
```java
void addMessage(String sessionId, ChatMessage message)
```
- **参数**：`sessionId` - 会话ID，`message` - 消息对象（含 role、content、timestamp）
- **返回值**：无
- **说明**：添加一条消息到对话历史

**addMessages()**：
```java
void addMessages(String sessionId, List<ChatMessage> messages)
```
- **参数**：`sessionId` - 会话ID，`messages` - 消息列表
- **返回值**：无
- **说明**：批量添加消息到对话历史

**clearHistory()**：
```java
void clearHistory(String sessionId)
```
- **参数**：`sessionId` - 会话ID
- **返回值**：无
- **说明**：清除会话的对话历史

**缓存策略**：
- **TTL**：会话历史保留 7 天（通过 Redis 的 expire 设置）
- **最大消息数**：每个会话最多保留 100 条消息（超出时自动 LRU 淘汰最早的消息）
- **序列化**：使用 `JsonUtil.toJson()` / `JsonUtil.fromJson()` 序列化 ChatMessage 对象
- **存储类型**：使用 Redis List 结构（LPUSH 添加消息，LRANGE 读取历史）

**addMessage执行流程**：
```
1. 构建 Redis Key: "chat:history:" + sessionId
2. 使用 JsonUtil.toJson(message) 序列化消息对象
3. 调用 redisService.lPush(key, jsonMessage) 添加到列表头部
4. 检查列表长度：redisService.lLen(key)
   └─ 若超过 100，使用 redisService.lTrim(key, 0, 99) 裁剪
5. 刷新 TTL：redisService.expire(key, 604800)
```

**getHistory执行流程**：
```
1. 构建 Redis Key: "chat:history:" + sessionId
2. 调用 redisService.lRange(key, 0, -1) 获取全部消息
3. 遍历列表，使用 JsonUtil.fromJson(json, ChatMessage.class) 反序列化
4. 反转列表（Redis List 是反向存储，需转为时间正序）
5. 返回 List<ChatMessage>
```

---

### 7.2.19 IStatisticsService / StatisticsServiceImpl

**类名**：`IStatisticsService`（接口） / `StatisticsServiceImpl`（实现类）

**职责**：使用统计服务，结合 Grafana+LangSmith 实现 AI 调用追踪和评估

**路径**：
- 接口：`com.zmbdp.chat.service.service.IStatisticsService`
- 实现：`com.zmbdp.chat.service.service.StatisticsServiceImpl`

**依赖注入**：
- `SysAiConversationMapper` - 对话记录 Mapper（用于 SQL 聚合统计）
- `SysAiOperationLogMapper` - 操作日志 Mapper
- `RedisService` - 缓存统计结果（5分钟TTL，降低数据库压力）

**核心方法**：

**getConversationStats()**：
```java
ConversationStatisticsVO getConversationStats()
```
- **参数**：无
- **返回值**：`ConversationStatisticsVO` - 对话统计数据（totalConversations、todayConversations、avgResponseTime、activeUsers、trendData）
- **说明**：获取对话统计（总对话数、今日对话数、平均响应时间等）

**getTopQuestions()**：
```java
List<HotQuestionVO> getTopQuestions(int limit)
```
- **参数**：`limit` - 返回数量（默认10）
- **返回值**：`List<HotQuestionVO>` - 热门问题列表（含 question、count、lastAskedTime）
- **说明**：获取热门问题 TOP N

**getUserStats()**：
```java
UserStatisticsVO getUserStats()
```
- **参数**：无
- **返回值**：`UserStatisticsVO` - 用户统计数据（含活跃用户数、使用频次、UserUsageVO 内嵌列表）
- **说明**：获取用户统计（活跃用户数、使用频次等）

**getToolUsageStats()**：
```java
ToolStatisticsVO getToolUsageStats()
```
- **参数**：无
- **返回值**：`ToolStatisticsVO` - 工具使用统计（含 ToolUsageDetailVO 内嵌列表，记录各工具调用次数、最后使用时间）
- **说明**：获取各 Agent 工具的调用次数

**getAiCallMetrics()**：
```java
AiMetricsVO getAiCallMetrics()
```
- **参数**：无
- **返回值**：`AiMetricsVO` - AI 调用指标（totalAiCalls、successRate、avgTokenUsage、totalTokenUsage、avgLatency、topModels）
- **说明**：获取 LangSmith 追踪的 AI 调用指标（token 消耗、成功率、平均耗时等）

**数据来源**：
- **API 调用统计**：后端提供统计 API 接口，供前端调用展示（对话统计、热门问题、用户统计、工具使用统计）
- **Grafana 监控**：通过 Prometheus 采集应用指标（请求量、响应时间、错误率），用于系统级监控告警
- **LangSmith 追踪**：集成 Spring AI 的 LangSmith Tracer，追踪每次 AI 调用的完整链路（Prompt、LLM 响应、工具调用），支持调试和评估
- **数据库统计**：基于 sys_ai_conversation 表的 SQL 聚合查询，提供业务层面的统计数据

**getConversationStats执行流程**：
```
1. 构建缓存 Key: "stats:conversation"
2. 尝试从 Redis 读取缓存：
   └─ 缓存命中 → 直接返回 ConversationStatisticsVO
3. 缓存未命中，执行数据库聚合查询：
   ├─ 总对话数：SELECT COUNT(*) FROM sys_ai_conversation WHERE status='SUCCESS'
   ├─ 今日对话数：SELECT COUNT(*) FROM sys_ai_conversation WHERE status='SUCCESS' AND create_date=今日
   ├─ 平均响应时间：SELECT AVG(response_time) FROM sys_ai_conversation WHERE status='SUCCESS'
   ├─ 活跃用户数：SELECT COUNT(DISTINCT user_id) FROM sys_ai_conversation WHERE create_date>=近7天
   └─ 趋势数据：按日期分组统计近7天每日对话数
4. 构建 ConversationStatisticsVO 对象
5. 写入缓存（TTL=300秒）
6. 返回结果
```

---

### 7.2.20 IAdminService / AdminServiceImpl

**类名**：`IAdminService`（接口） / `AdminServiceImpl`（实现类）

**职责**：B端管理服务（AI 业务相关），由 chat-service 提供给 admin-service 通过 Feign 调用

**路径**：
- 接口：`com.zmbdp.chat.service.service.IAdminService`
- 实现：`com.zmbdp.chat.service.service.AdminServiceImpl`

**说明**：用户管理（sys_user 表 CRUD）已在 `zmbdp-admin-service` 中实现，本服务不再重复开发。脚手架未实现角色管理（无 sys_role 表）和细粒度权限控制（无 sys_permission 表）。

**依赖注入**：
- `IKnowledgeService` - 知识库管理服务
- `IKnowledgeLoaderService` - 知识加载与同步服务
- `IModelService` - 模型管理服务
- `SnowflakeIdService` - ID 生成器
- `AESUtil` - API Key 加密
- `DesensitizeUtil` - API Key 脱敏

**核心方法**：

**知识源管理**：
```java
BasePageVO<KnowledgeSourceVO> listKnowledgeSources(Integer pageNo, Integer pageSize, String type);
KnowledgeSourceVO getKnowledgeSource(Long id);
KnowledgeSourceVO createKnowledgeSource(KnowledgeSourceReqDTO dto);
void updateKnowledgeSource(Long id, KnowledgeSourceReqDTO dto);
void deleteKnowledgeSource(Long id);
```

**AI配置管理**：
```java
AiConfigVO getAiConfig();
void updateAiConfig(AiConfigDTO dto);
List<ModelVO> listModels();
boolean testAiConnection(String model, String message);
```

**工具管理**：
```java
List<ToolVO> listTools();
void updateToolConfig(String name, ToolConfigDTO dto);
ToolTestResultVO testTool(String name, ToolTestReqDTO dto);
```

**createKnowledgeSource执行流程**：
```
1. 参数校验：name 非空、path 非空、type 合法（doc/javadoc/config/code）
2. 校验 path 是否存在（FileUtil.exist）
3. 校验 name 是否重复（查询 sys_ai_knowledge_source 表）
4. 构建 SysAiKnowledgeSource 实体：
   ├─ id: snowflakeIdService.nextId()
   ├─ name、path、type、enabled（默认1）
   ├─ chunk_size（默认500）、chunk_overlap（默认50）
   ├─ create_date、update_date（当前日期 YYYYMMDD 格式）
   └─ create_time、update_time（当前时间戳）
5. 调用 sysAiKnowledgeSourceMapper.insert()
6. 记录操作日志到 sys_ai_operation_log（operation=CREATE, module=knowledge, target_id）
7. 构建 KnowledgeSourceVO 返回
```

**getAiConfig执行流程**：
```
1. 从 sys_ai_config 表查询所有 enabled=1 的配置项
2. 构建 AiConfigVO：
   ├─ defaultModel: 配置项 default_model 的值
   ├─ defaultVisionModel: 配置项 default_vision_model 的值
   ├─ temperature、maxTokens、embeddingModel、topK、enableRag、enableTools
   └─ apiKey: 使用 DesensitizeUtil.desensitize(原Key) 脱敏显示（仅前后4位）
3. 返回 AiConfigVO
```

**updateAiConfig执行流程**：
```
1. 参数校验：temperature 范围 0-1、maxTokens 范围 1-8192、topK 范围 1-20
2. 遍历 AiConfigDTO 的非空字段，更新 sys_ai_config 表对应配置项
3. 若 apiKey 非空：
   ├─ 使用 AESUtil.encrypt(apiKey) 加密
   └─ 更新 sys_ai_config 表的 api_key 配置项
4. 刷新 Nacos 配置（触发 @RefreshScope 重新加载）
5. 记录操作日志到 sys_ai_operation_log（operation=UPDATE, module=ai_config）
6. 返回 void
```

**listTools执行流程**：
```
1. 从 sys_ai_tool_config 表查询所有工具配置
2. 遍历工具配置，构建 ToolVO：
   ├─ name、description、enabled
   ├─ config: 解析 config_json 为 Map<String, Object>
   ├─ lastUsedTime: 从 sys_ai_operation_log 查询最近一次调用时间
   └─ usedCount: 从 sys_ai_operation_log 查询调用次数
3. 返回 List<ToolVO>
```

**updateToolConfig执行流程**：
```
1. 校验 name 是否存在于 sys_ai_tool_config 表
2. 更新 sys_ai_tool_config 表：
   ├─ enabled: 更新启用状态
   └─ config_json: 使用 JsonUtil.toJson(dto.getConfig()) 序列化
3. 若 enabled 从 0 变为 1：触发 ToolConfig 刷新，注册到 ToolExecutor
4. 若 enabled 从 1 变为 0：从 ToolExecutor 移除该工具
5. 记录操作日志到 sys_ai_operation_log（operation=UPDATE, module=tools, target_id=name）
6. 返回 void
```

---

### 7.2.21 IHistoryService / HistoryServiceImpl

**类名**：`IHistoryService`（接口） / `HistoryServiceImpl`（实现类）

**职责**：对话历史管理，提供历史列表查询、会话详情查询、会话删除；持久化对话记录到 MySQL（`sys_ai_conversation` 表）

**路径**：
- 接口：`com.zmbdp.chat.service.service.IHistoryService`
- 实现：`com.zmbdp.chat.service.service.HistoryServiceImpl`

**依赖注入**：
- `SysAiConversationMapper` - 对话记录 Mapper
- `IChatMemoryService` - Redis 对话记忆服务
- `SnowflakeIdService` - ID 生成器

**核心方法**：

```java
BasePageVO<HistoryVO> getHistoryList(Long userId, Integer pageNo, Integer pageSize);
HistoryDetailVO getSessionHistory(String sessionId);
void deleteSession(String sessionId);
void saveConversation(SysAiConversation conversation);
```

**getHistoryList执行流程**：
```
1. 从 sys_ai_conversation 表按 user_id 分组查询会话列表
   ├─ SELECT session_id, MAX(create_time) as last_time, COUNT(*) as msg_count, 
   │         MAX(model) as model, MAX(user_message) as last_message
   │   FROM sys_ai_conversation WHERE user_id=#{userId} AND status='SUCCESS'
   │   GROUP BY session_id ORDER BY last_time DESC
2. 分页处理（pageNo、pageSize）
3. 构建 BasePageVO<HistoryVO>：
   ├─ list: List<HistoryVO>（sessionId、lastMessage、timestamp、model、messageCount）
   ├─ totals: 总会话数
   └─ totalPages: 总页数
4. 返回结果
```

**getSessionHistory执行流程**：
```
1. 从 Redis（IChatMemoryService.getHistory）获取会话消息列表
2. 若 Redis 无数据（已过期），从 MySQL 查询：
   └─ SELECT * FROM sys_ai_conversation WHERE session_id=#{sessionId} ORDER BY create_time
3. 构建 HistoryDetailVO（sessionId、messages: List<ChatMessageVO>）
4. 返回结果
```

**deleteSession执行流程**：
```
1. 从 Redis 删除会话历史：chatMemoryService.clearHistory(sessionId)
2. 从 MySQL 逻辑删除会话记录：
   └─ UPDATE sys_ai_conversation SET status='DELETED' WHERE session_id=#{sessionId}
3. 返回 void
```

---

### 7.2.22 IKnowledgeService / KnowledgeServiceImpl

**类名**：`IKnowledgeService`（接口） / `KnowledgeServiceImpl`（实现类）

**职责**：知识库管理（B端 CRUD），管理知识源、文档；由 admin-service 通过 Feign 调用

**路径**：
- 接口：`com.zmbdp.chat.service.service.IKnowledgeService`
- 实现：`com.zmbdp.chat.service.service.KnowledgeServiceImpl`

**依赖注入**：
- `SysAiKnowledgeSourceMapper` - 知识源 Mapper
- `SysAiDocumentMapper` - 文档 Mapper
- `IKnowledgeLoaderService` - 知识加载与同步服务
- `IVectorStoreService` - 向量存储服务
- `SnowflakeIdService` - ID 生成器

**核心方法**：

```java
// 知识源管理
BasePageVO<KnowledgeSourceVO> listSources(Integer pageNo, Integer pageSize, String type);
KnowledgeSourceVO getSource(Long id);
KnowledgeSourceVO createSource(KnowledgeSourceReqDTO dto);
void updateSource(Long id, KnowledgeSourceReqDTO dto);
void deleteSource(Long id);

// 文档管理
BasePageVO<KnowledgeDocumentVO> listDocuments(Integer pageNo, Integer pageSize, Long sourceId, String status);
KnowledgeDocumentVO getDocument(Long id);
void deleteDocument(Long id);

// 同步
SyncResultVO sync(SyncReqDTO dto);
```

**createSource执行流程**：
```
1. 参数校验：name 非空、path 非空、type 合法
2. 校验 path 是否存在（FileUtil.exist）
3. 校验 name 是否重复
4. 构建 SysAiKnowledgeSource 实体：
   ├─ id: snowflakeIdService.nextId()
   ├─ name、path、type、enabled（默认1）
   ├─ chunk_size（默认500）、chunk_overlap（默认50）
   └─ create_date、update_date（YYYYMMDD 格式）
5. 调用 mapper.insert()
6. 构建 KnowledgeSourceVO 返回
```

**deleteSource执行流程**：
```
1. 查询知识源下所有文档（sys_ai_document WHERE knowledge_source_id=#{id}）
2. 遍历每个文档：
   ├─ 调用 vectorStoreService.deleteByDocumentId(documentId) 删除 Milvus 分块
   └─ 更新 sys_ai_document status='DELETED'（软删除）
3. 删除知识源：UPDATE sys_ai_knowledge_source SET enabled=0 WHERE id=#{id}
4. 返回 void
```

**sync执行流程**：
```
1. 委托给 knowledgeLoaderService.syncKnowledge(dto.force)
2. 返回 SyncResultVO
```

---

### 7.2.23 AiConfig

**类名**：`AiConfig`

**职责**：AI相关配置，包括ChatClient、EmbeddingModel等

**路径**：`com.zmbdp.chat.service.config.AiConfig`

**Bean定义**：

| Bean名称 | 返回类型 | 说明 |
|---------|---------|------|
| `chatClient()` | `ChatClient` | AI对话客户端 |
| `embeddingModel()` | `EmbeddingModel` | 向量嵌入模型 |

**配置项**：
```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        model: qwen-max
        options:
          temperature: 0.7
          max-tokens: 4096
      embedding:
        model: text-embedding-v1
```

---

### 7.2.24 chat-service 内部接口鉴权机制

**说明**：脚手架不使用 Spring Security 的 `SecurityConfig`，认证统一由 Gateway 层的 `AuthFilter` 完成。`zmbdp-chat-service` 不直接暴露给前端，不经过网关，因此**不需要 JWT 认证**。

**鉴权机制**：

| 接口类型 | 调用方 | 鉴权方式 | 说明 |
|---------|-------|---------|------|
| Feign 接口（同步） | portal-service、admin-service | 无需鉴权 | 依靠 Docker 内部网络隔离，仅同网络内的服务可调用 |
| HTTP SSE 端点（流式） | portal-service（WebClient） | 无需鉴权 | 同上，内部网络隔离 |
| `/actuator/**` | 运维监控 | 无需鉴权 | 健康检查接口，不暴露敏感信息 |

**安全保障**：
1. **网络隔离**：chat-service 仅在 Docker 内部网络中可见，外部无法直接访问 `http://{chat-service-ip}:18084`
2. **网关统一鉴权**：前端请求经 Gateway 时，由 AuthFilter 统一完成 JWT 校验和用户来源检查
3. **服务信任**：portal-service 和 admin-service 已通过 Gateway 鉴权，chat-service 信任内部调用方
4. **无需引入 `share-token` 配置**：chat-service 不做 Token 校验，但为保持与脚手架一致（`TokenService` 可能被其他组件依赖），仍建议引入 `share-token` 共享配置

**注意**：如果未来需要 chat-service 直接暴露给前端（不推荐），则需要新增独立的鉴权机制。当前架构下，所有前端请求必须经 Gateway → portal-service/admin-service → chat-service。

---

### 7.2.25 TokenService（使用项目现有）

**类名**：`TokenService`

**职责**：Token的创建、验证、刷新、删除，基于JWT和Redis实现

**路径**：`com.zmbdp.common.security.service.TokenService`

**说明**：AI模块直接使用项目现有的 `TokenService`，无需重新定义。

**核心方法**：

**createToken()**：
```java
public TokenDTO createToken(LoginUserDTO loginUserDTO, String secret)
```
- **参数**：`LoginUserDTO` 用户信息、`secret` JWT签名密钥
- **返回值**：`TokenDTO`（包含 `accessToken` 和 `expires`）
- **说明**：为用户创建登录Token，自动存储到Redis

**getLoginUser()**：
```java
public LoginUserDTO getLoginUser(String token, String secret)
```
- **参数**：JWT Token、签名密钥
- **返回值**：`LoginUserDTO` 用户信息
- **说明**：从Token中解析用户信息，需同时验证JWT和Redis

**delLoginUser()**：
```java
public void delLoginUser(String token, String secret)
```
- **参数**：JWT Token、签名密钥
- **说明**：删除用户登录状态（从Redis中移除）

**verifyToken()**：
```java
public void verifyToken(LoginUserDTO loginUserDTO)
```
- **参数**：`LoginUserDTO` 用户信息
- **说明**：验证并续期Token（剩余时间少于120分钟自动续期）

**Token配置**：

| 配置项 | 值 | 说明 |
|-------|-----|------|
| `expireTime` | 720分钟（12小时） | Token在Redis中的过期时间 |
| `refreshTime` | 120分钟 | 续期阈值，剩余时间少于此时自动续期 |
| `signingAlgorithm` | HS256 | 签名算法 |
| `signingKey` | 配置在Nacos | 签名密钥 |

**Redis Key格式**：`logintoken:{uuid}`

**使用示例（登录）**：
```java
LoginUserDTO loginUserDTO = new LoginUserDTO();
loginUserDTO.setUserId(user.getId());
loginUserDTO.setUserName(user.getUsername());
// userFrom 由前端传入，后端使用 UserConstants 常量进行校验
// 可选值：UserConstants.USER_FROM_TU_B("sys") 或 UserConstants.USER_FROM_TU_C("app")

TokenDTO tokenDTO = tokenService.createToken(loginUserDTO, jwtSecret);
LoginVO loginVO = new LoginVO();
loginVO.setToken(tokenDTO.getAccessToken());
loginVO.setExpireTime(System.currentTimeMillis() + tokenDTO.getExpires() * 60 * 1000);
loginVO.setUser(userVO);
return Result.success(loginVO);
```

**UserConstants 用户来源常量**：
- `UserConstants.USER_FROM_TU_B = "sys"` - B端用户（管理员）
- `UserConstants.USER_FROM_TU_C = "app"` - C端用户（前端用户）

**说明**：userFrom字段由前端在登录时传入，后端不硬编码设置，只做格式校验。

---

### 7.2.26 B端AI接口的权限控制（扩展 AuthFilter）

> **重要说明**：脚手架代码库中**不存在** `@RequiresPermission` 注解，也未实现细粒度 RBAC 权限控制。sys_user 表的 `identity` 字段（`super_admin`/`platform_admin`）目前仅作标识存储，没有对应的权限校验逻辑。AI 模块的 B 端接口权限控制需要通过**扩展 Gateway 的 AuthFilter** 实现。

**现有 AuthFilter 的实际行为**（[AuthFilter.java#L94-L100](file:///d:/GitHub/scaffold-ai-assistant/zmbdp-gateway/src/main/java/com/zmbdp/gateway/filter/AuthFilter.java#L94-L100)）：

```java
String userFrom = JwtUtil.getUserFrom(claims);
if (url.contains(HttpConstants.SYS_USER_PATH) &&        // SYS_USER_PATH = "sys_user"
    !UserConstants.USER_FROM_TU_B.equals(userFrom)) {    // 仅当 userFrom != "sys" 时拒绝
    return unauthorizedResponse(exchange, ResultCode.TOKEN_CHECK_FAILED);
}
```

- 仅对**路径包含字符串 `sys_user`** 的请求做来源校验
- 其他所有非白名单路径（如 `/admin/app_user/**`、`/admin/dictionary/**`、`/portal/user/**`）只要有有效 Token 即可访问，**不区分用户来源**

**AI 模块需要扩展 AuthFilter**：

```java
// 在 AuthFilter 中新增对 AI 管理 B 端路径的来源校验
private static final String[] ADMIN_AI_PATHS = {
    "/admin/knowledge", "/admin/ai", "/admin/statistics", "/admin/tools"
};

private boolean isAdminAiPath(String url) {
    for (String path : ADMIN_AI_PATHS) {
        if (url.contains(path)) {
            return true;
        }
    }
    return false;
}

// 在原有 sys_user 校验后追加
if (isAdminAiPath(url) && !UserConstants.USER_FROM_TU_B.equals(userFrom)) {
    return unauthorizedResponse(exchange, ResultCode.TOKEN_CHECK_FAILED);
}
```

**权限控制策略**：

| 接口路径 | C 端用户（userFrom=app） | B 端用户（userFrom=sys） | 实现方式 |
|---------|----------------------|----------------------|---------|
| `/portal/chat/**`、`/portal/history/**`（C端 AI 接口） | ✅ 放行 | ✅ 放行 | 现有 AuthFilter 不拦截 |
| `/admin/knowledge/**`、`/admin/ai/**`、`/admin/statistics/**`、`/admin/tools/**` | ❌ 拒绝 | ✅ 放行 | **需扩展 AuthFilter** |
| `/admin/sys_user/**`（现有 B 端用户管理） | ❌ 拒绝 | ✅ 放行 | 现有 AuthFilter |
| 白名单路径（如 `/admin/codeLogin`） | ✅ 放行 | ✅ 放行 | `security.ignore.whites` |

**未来扩展**：若后续需要实现 `@RequiresPermission` 式的细粒度权限控制，需要**新增**该注解及对应的 AOP 切面（脚手架当前没有，需作为 v2.0 功能开发）。

---

## 7.3 DTO详细设计

### 7.3.1 ChatReqDTO

**类名**：`ChatReqDTO`

**路径**：`com.zmbdp.chat.api.chat.domain.dto.ChatReqDTO`

**字段**：

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `message` | String | 是 | 用户提问内容 |
| `sessionId` | String | 否 | 会话ID，用于对话记忆 |
| `model` | String | 否 | 指定模型（默认使用配置的模型） |
| `temperature` | Double | 否 | 温度参数（0-1），默认0.7 |
| `topK` | Integer | 否 | RAG检索数量，默认5 |

---

### 7.3.2 ChatVO

**类名**：`ChatVO`

**路径**：`com.zmbdp.chat.api.chat.domain.vo.ChatVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `answer` | String | AI生成的回答内容 |
| `sessionId` | String | 会话ID |
| `sources` | List<SourceInfo> | 回答引用的来源文档列表 |
| `toolUsage` | ToolUsageInfo | 工具调用信息（如果使用了工具） |
| `model` | String | 使用的模型名称 |
| `timestamp` | Long | 响应时间戳 |

**SourceInfo**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `sourceType` | String | 来源类型（doc/javadoc/config/code） |
| `sourcePath` | String | 来源文件路径 |
| `content` | String | 引用内容摘要 |

**ToolUsageInfo**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `toolName` | String | 工具名称 |
| `parameters` | Map<String, Object> | 工具参数 |
| `result` | String | 工具执行结果 |

---

### 7.3.3 KnowledgeSourceReqDTO

**类名**：`KnowledgeSourceReqDTO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.dto.KnowledgeSourceReqDTO`

**字段**：

| 字段名 | 类型 | 必填 | 校验 | 说明 |
|-------|------|------|------|------|
| `name` | String | 是 | @NotBlank, @Size(max=100) | 知识源名称 |
| `path` | String | 是 | @NotBlank, @Size(max=500) | 文件路径（知识源根目录绝对路径） |
| `type` | String | 是 | @Pattern(regexp="^(doc|javadoc|config)$") | 类型（doc/javadoc/config） |
| `enabled` | Boolean | 否 | - | 是否启用，默认true |
| `chunkSize` | Integer | 否 | @Min(100), @Max(5000) | 分块大小（字符数），默认500 |
| `chunkOverlap` | Integer | 否 | @Min(0), @Max(500) | 分块重叠大小（字符数），默认50 |

---

### 7.3.4 KnowledgeSourceVO

**类名**：`KnowledgeSourceVO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.vo.KnowledgeSourceVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `id` | Long | 知识源ID |
| `name` | String | 知识源名称 |
| `path` | String | 文件路径 |
| `type` | String | 类型 |
| `enabled` | Boolean | 是否启用 |
| `chunkSize` | Integer | 分块大小（字符数） |
| `chunkOverlap` | Integer | 分块重叠大小（字符数） |
| `lastSyncDate` | Long | 最后同步日期（格式：20260712，为空表示未同步） |
| `createDate` | Long | 创建日期（格式：20260712） |
| `updateDate` | Long | 更新日期（格式：20260712） |

---

### 7.3.5 KnowledgeSourceListReqDTO

**类名**：`KnowledgeSourceListReqDTO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.dto.KnowledgeSourceListReqDTO`

**说明**：继承自 `BasePageReqDTO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `pageNo` | Integer | 页码，默认1（继承） |
| `pageSize` | Integer | 每页数量，默认10（继承） |
| `type` | String | 类型过滤 |

---

### 7.3.6 AiConfigDTO

**类名**：`AiConfigDTO`

**路径**：`com.zmbdp.chat.api.config.domain.dto.AiConfigDTO`

**说明**：AI配置更新请求DTO，字段为空表示不更新该配置项

**字段**：

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `defaultModel` | String | 否 | 默认文本模型名称（如 qwen-max） |
| `defaultVisionModel` | String | 否 | 默认图文模型名称（如 qwen-vl-max） |
| `temperature` | Double | 否 | 温度参数（0-2） |
| `maxTokens` | Integer | 否 | 最大Token数（1-16384） |
| `embeddingModel` | String | 否 | 向量嵌入模型名称 |
| `topK` | Integer | 否 | RAG检索默认数量（1-20） |
| `enableRag` | Boolean | 否 | 是否启用RAG检索 |
| `enableTools` | Boolean | 否 | 是否启用Agent工具调用 |

---

### 7.3.7 AiConfigVO

**类名**：`AiConfigVO`

**路径**：`com.zmbdp.chat.api.config.domain.vo.AiConfigVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `defaultModel` | String | 默认文本模型名称 |
| `defaultVisionModel` | String | 默认图文模型名称 |
| `temperature` | Double | 温度参数 |
| `maxTokens` | Integer | 最大Token数 |
| `embeddingModel` | String | 向量嵌入模型名称 |
| `topK` | Integer | RAG检索默认数量 |
| `enableRag` | Boolean | 是否启用RAG检索 |
| `enableTools` | Boolean | 是否启用Agent工具调用 |
| `apiKey` | String | API Key（脱敏显示，仅展示前后4位，如 `sk-1***wxyz`） |

---

### 7.3.8 ConversationStatisticsVO

**类名**：`ConversationStatisticsVO`

**路径**：`com.zmbdp.chat.api.statistics.domain.vo.ConversationStatisticsVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `totalConversations` | Long | 总对话次数 |
| `todayConversations` | Long | 今日对话次数 |
| `avgResponseTime` | Double | 平均响应时间（毫秒） |
| `activeUsers` | Long | 活跃用户数 |
| `trendData` | List<TrendItem> | 趋势数据（近7天或30天） |

**TrendItem**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `date` | String | 日期（格式：yyyy-MM-dd） |
| `count` | Long | 对话次数 |

---

### 7.3.9 AiMetricsVO

**类名**：`AiMetricsVO`

**路径**：`com.zmbdp.chat.api.statistics.domain.vo.AiMetricsVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `totalAiCalls` | Long | AI调用总次数 |
| `successRate` | Double | AI调用成功率（%，0-100） |
| `avgTokenUsage` | Long | 平均Token消耗 |
| `totalTokenUsage` | Long | 总Token消耗 |
| `avgLatency` | Double | 平均延迟（毫秒） |
| `topModels` | List<ModelUsage> | 模型使用排行 |

**ModelUsage**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `model` | String | 模型名称 |
| `count` | Long | 调用次数 |

---

### 7.3.10 ChatStreamReqDTO

**类名**：`ChatStreamReqDTO`

**路径**：`com.zmbdp.chat.api.chat.domain.dto.ChatStreamReqDTO`

**说明**：流式文本对话请求DTO，由 portal-service 通过 WebClient 调用 chat-service SSE 端点时使用（非 Feign 接口）

**字段**：

| 字段名 | 类型 | 必填 | 校验 | 说明 |
|-------|------|------|------|------|
| `message` | String | 是 | @NotBlank, @Size(max=10000) | 用户提问内容 |
| `sessionId` | String | 否 | @Size(max=50) | 会话ID，不传则新建会话 |
| `prompt` | String | 是 | @NotBlank | 完整Prompt（由portal-service拼接，包含system prompt + 上下文 + 用户提问） |
| `model` | String | 否 | - | 模型名称，为空使用默认模型（default_model配置） |
| `temperature` | Double | 否 | @Min(0), @Max(2) | 温度参数，为空使用默认值（0.7） |

---

### 7.3.11 ChatWithImageStreamReqDTO

**类名**：`ChatWithImageStreamReqDTO`

**路径**：`com.zmbdp.chat.api.chat.domain.dto.ChatWithImageStreamReqDTO`

**说明**：流式图文对话请求DTO，由 portal-service 通过 WebClient 调用 chat-service SSE 端点时使用

**字段**：

| 字段名 | 类型 | 必填 | 校验 | 说明 |
|-------|------|------|------|------|
| `message` | String | 是 | @NotBlank, @Size(max=10000) | 用户提问内容 |
| `images` | List<String> | 是 | @NotEmpty | 图片Base64编码列表或图片URL列表 |
| `sessionId` | String | 否 | @Size(max=50) | 会话ID |
| `prompt` | String | 是 | @NotBlank | 完整Prompt |
| `model` | String | 否 | - | 模型名称，为空使用默认图文模型 |
| `temperature` | Double | 否 | @Min(0), @Max(2) | 温度参数 |

---

### 7.3.12 RetrieveReqDTO

**类名**：`RetrieveReqDTO`

**路径**：`com.zmbdp.chat.api.chat.domain.dto.RetrieveReqDTO`

**说明**：RAG检索请求DTO，由 portal-service 通过 Feign 调用 ChatApi.retrieveContext() 时使用

**字段**：

| 字段名 | 类型 | 必填 | 校验 | 说明 |
|-------|------|------|------|------|
| `question` | String | 是 | @NotBlank | 用户提问内容（用于生成查询向量） |
| `topK` | Integer | 否 | @Min(1), @Max(20) | 返回最相关的文档数量，为空使用默认值5 |
| `filters` | Map<String,String> | 否 | - | 元数据过滤条件（如按module、type过滤） |

---

### 7.3.13 DocumentVO

**类名**：`DocumentVO`

**路径**：`com.zmbdp.chat.api.chat.domain.vo.DocumentVO`

**说明**：RAG检索返回的文档信息，供 portal-service 拼接 Prompt 使用

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `documentId` | Long | 文档ID（关联MySQL sys_ai_document表的id） |
| `title` | String | 文档标题 |
| `content` | String | 文档分块内容（检索命中的分块，非完整文档） |
| `sourceType` | String | 来源类型（doc/javadoc/config/code） |
| `sourcePath` | String | 来源文件路径 |
| `module` | String | 所属模块 |
| `category` | String | 功能类别 |
| `score` | Double | 相似度得分（0-1，越高越相关） |
| `chunkIndex` | Integer | 分块序号 |

---

### 7.3.14 HistoryVO

**类名**：`HistoryVO`

**路径**：`com.zmbdp.chat.api.history.domain.vo.HistoryVO`

**说明**：对话历史列表项VO

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `sessionId` | String | 会话ID |
| `lastMessage` | String | 最后一条消息内容（截断显示，最多100字符） |
| `timestamp` | Long | 最后对话时间戳（毫秒） |
| `model` | String | 使用的模型名称 |
| `messageCount` | Integer | 会话消息总数 |

---

### 7.3.15 HistoryDetailVO

**类名**：`HistoryDetailVO`

**路径**：`com.zmbdp.chat.api.history.domain.vo.HistoryDetailVO`

**说明**：会话详情VO，包含完整的消息列表

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `sessionId` | String | 会话ID |
| `messages` | List<ChatMessageVO> | 消息列表（按时间正序） |

**ChatMessageVO**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `role` | String | 角色（user/assistant） |
| `content` | String | 消息内容 |
| `timestamp` | Long | 消息时间戳（毫秒） |
| `hasImage` | Boolean | 是否包含图片 |
| `sources` | List<SourceInfo> | 引用来源（仅assistant消息有） |
| `model` | String | 使用的模型（仅assistant消息有） |
| `toolUsage` | ToolUsageInfo | 工具调用信息（仅assistant消息有，可选） |

---

### 7.3.16 KnowledgeDocumentVO

**类名**：`KnowledgeDocumentVO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.vo.KnowledgeDocumentVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `id` | Long | 文档ID |
| `knowledgeSourceId` | Long | 所属知识源ID |
| `title` | String | 文档标题 |
| `path` | String | 文件路径 |
| `content` | String | 完整文档内容（列表查询时不返回，详情查询时返回） |
| `type` | String | 类型（doc/javadoc/config） |
| `module` | String | 所属模块 |
| `category` | String | 功能类别 |
| `status` | String | 状态（ACTIVE/DELETED） |
| `version` | Integer | 版本号 |
| `chunkCount` | Integer | 分块数量 |
| `hash` | String | 文件内容哈希 |
| `createDate` | Long | 创建日期（格式：20260712） |
| `updateDate` | Long | 更新日期（格式：20260712） |

---

### 7.3.17 SyncReqDTO

**类名**：`SyncReqDTO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.dto.SyncReqDTO`

**字段**：

| 字段名 | 类型 | 必填 | 校验 | 说明 |
|-------|------|------|------|------|
| `sourceType` | String | 否 | @Pattern(regexp="^(all|doc|javadoc|config)$") | 同步类型，默认all |
| `force` | Boolean | 否 | - | 是否强制重新同步（跳过哈希检查），默认false |

---

### 7.3.18 SyncResultVO

**类名**：`SyncResultVO`

**路径**：`com.zmbdp.chat.api.knowledge.domain.vo.SyncResultVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `totalDocuments` | Integer | 处理的文档总数 |
| `addedDocuments` | Integer | 新增文档数量 |
| `updatedDocuments` | Integer | 更新文档数量 |
| `deletedDocuments` | Integer | 删除文档数量 |
| `skippedDocuments` | Integer | 跳过文档数量（哈希未变） |
| `syncDuration` | Long | 同步耗时（毫秒） |
| `errors` | List<String> | 同步过程中的错误信息列表 |

---

### 7.3.19 ModelVO

**类名**：`ModelVO`

**路径**：`com.zmbdp.chat.api.config.domain.vo.ModelVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `name` | String | 模型名称（如 qwen-max） |
| `provider` | String | 供应商（alibaba/openai/zhipu/gemini） |
| `type` | String | 模型类型（TEXT_ONLY/TEXT_AND_IMAGE/EMBEDDING），见 ModelType 枚举 |
| `capabilities` | List<String> | 模型能力列表（TEXT/IMAGE/EMBEDDING/FUNCTION_CALL） |
| `enabled` | Boolean | 是否启用 |
| `isDefault` | Boolean | 是否为默认模型 |
| `description` | String | 模型描述 |

---

### 7.3.20 ToolVO

**类名**：`ToolVO`

**路径**：`com.zmbdp.chat.api.tool.domain.vo.ToolVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `name` | String | 工具类名（如 ReadFileTool） |
| `description` | String | 工具描述 |
| `enabled` | Boolean | 是否启用 |
| `config` | Map<String, Object> | 工具配置参数（如 maxFileSize、pathWhitelist 等） |
| `lastUsedTime` | Long | 最后使用时间戳（毫秒，为空表示从未使用） |
| `usedCount` | Long | 累计使用次数 |

---

### 7.3.21 ToolConfigDTO

**类名**：`ToolConfigDTO`

**路径**：`com.zmbdp.chat.api.tool.domain.dto.ToolConfigDTO`

**字段**：

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `enabled` | Boolean | 否 | 是否启用工具 |
| `config` | Map<String, Object> | 否 | 工具配置参数（与工具类型相关，不同工具有不同配置项） |

---

### 7.3.22 ToolTestReqDTO

**类名**：`ToolTestReqDTO`

**路径**：`com.zmbdp.chat.api.tool.domain.dto.ToolTestReqDTO`

**字段**：

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| `params` | Map<String, Object> | 是 | 测试参数（不同工具有不同参数，如 ReadFileTool 需传 filePath） |

---

### 7.3.23 ToolTestResultVO

**类名**：`ToolTestResultVO`

**路径**：`com.zmbdp.chat.api.tool.domain.vo.ToolTestResultVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `success` | Boolean | 测试是否成功 |
| `result` | String | 工具执行结果（成功时） |
| `error` | String | 错误信息（失败时） |
| `latency` | Long | 执行耗时（毫秒） |

---

### 7.3.24 HotQuestionVO

**类名**：`HotQuestionVO`

**路径**：`com.zmbdp.chat.api.statistics.domain.vo.HotQuestionVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `question` | String | 问题内容（截断显示，最多100字符） |
| `count` | Long | 被问次数 |
| `category` | String | 问题分类（架构/功能/API/配置/部署等） |

---

### 7.3.25 UserStatisticsVO

**类名**：`UserStatisticsVO`

**路径**：`com.zmbdp.chat.api.statistics.domain.vo.UserStatisticsVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `totalUsers` | Long | 总用户数 |
| `activeUsers` | Long | 活跃用户数（近7天有对话） |
| `newUsersToday` | Long | 今日新增用户数 |
| `avgConversationsPerUser` | Double | 平均每用户对话次数 |
| `topUsers` | List<UserUsageVO> | 活跃用户排行TOP10 |

**UserUsageVO**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `userId` | Long | 用户ID |
| `username` | String | 用户名 |
| `conversationCount` | Long | 对话次数 |
| `lastActiveTime` | Long | 最后活跃时间戳 |

---

### 7.3.26 ToolStatisticsVO

**类名**：`ToolStatisticsVO`

**路径**：`com.zmbdp.chat.api.statistics.domain.vo.ToolStatisticsVO`

**字段**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `totalCalls` | Long | 工具总调用次数 |
| `successRate` | Double | 工具调用成功率（%） |
| `toolUsageList` | List<ToolUsageDetailVO> | 各工具使用详情 |

**ToolUsageDetailVO**：

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `toolName` | String | 工具名称 |
| `callCount` | Long | 调用次数 |
| `successCount` | Long | 成功次数 |
| `failCount` | Long | 失败次数 |
| `avgLatency` | Double | 平均耗时（毫秒） |

---

---

## 7.4 数据库设计

> **说明**：用户管理相关表已在 `zmbdp-admin-service` 的 `sys_user` 表中实现（identity字段标识身份，权限通过Gateway的userFrom字段控制），C端用户使用 `app_user` 表，本服务直接复用，无需重复建表。本服务仅需创建AI业务相关的表。

### 7.4.1 知识源表（sys_ai_knowledge_source）

**实体类**：`SysAiKnowledgeSource`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiKnowledgeSource`

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 知识源ID（雪花算法） |
| `name` | VARCHAR(100) | NOT NULL, UNIQUE | 知识源名称（不可重复） |
| `path` | VARCHAR(500) | NOT NULL | 文件路径（知识源根目录绝对路径） |
| `type` | VARCHAR(20) | NOT NULL | 类型（doc/javadoc/config），枚举值见 DocumentType |
| `enabled` | TINYINT(1) | NOT NULL, DEFAULT 1 | 是否启用（1=启用，0=禁用） |
| `chunk_size` | INT | DEFAULT 500 | 分块大小（字符数），用于文档分块处理 |
| `chunk_overlap` | INT | DEFAULT 50 | 分块重叠大小（字符数），保证上下文连续性 |
| `last_sync_date` | BIGINT | | 最后同步日期（格式：20260712，首次同步前为空） |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `update_date` | BIGINT | NOT NULL | 更新日期（格式：20260712） |

**索引设计**：
- `uk_name`：`name`（唯一索引，防止知识源重名）
- `idx_path`：`path`（按路径查询知识源）
- `idx_type`：`type`（按类型过滤）
- `idx_enabled`：`enabled`（快速筛选启用的知识源）
- `idx_last_sync_date`：`last_sync_date`（范围查询，用于增量同步）
- `idx_create_date`：`create_date`（范围查询）

### 7.4.2 文档表（sys_ai_document）

**实体类**：`SysAiDocument`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiDocument`

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 文档ID（雪花算法） |
| `knowledge_source_id` | BIGINT | FOREIGN KEY | 所属知识源ID |
| `title` | VARCHAR(200) | NOT NULL | 文档标题 |
| `path` | VARCHAR(500) | UNIQUE | 文件路径 |
| `content` | LONGTEXT | | 完整文档内容 |
| `type` | VARCHAR(20) | NOT NULL | 类型（doc/javadoc/config） |
| `module` | VARCHAR(100) | | 所属模块 |
| `category` | VARCHAR(100) | | 功能类别 |
| `status` | VARCHAR(20) | DEFAULT 'ACTIVE' | 状态（ACTIVE/DELETED） |
| `version` | INT | DEFAULT 1 | 版本号 |
| `hash` | VARCHAR(64) | | 文件内容哈希（用于增量更新） |
| `chunk_count` | INT | DEFAULT 0 | 分块数量 |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `update_date` | BIGINT | NOT NULL | 更新日期（格式：20260712） |

**索引设计**：
- `uk_path`：`path`（唯一索引）
- `idx_knowledge_source_id`：`knowledge_source_id`
- `idx_type`：`type`
- `idx_module`：`module`
- `idx_category`：`category`
- `idx_status`：`status`
- `idx_create_date`：`create_date`（范围查询）

### 7.4.3 操作日志表（sys_ai_operation_log）

**实体类**：`SysAiOperationLog`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiOperationLog`

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 日志ID（雪花算法） |
| `user_id` | BIGINT | NOT NULL | 操作用户ID（关联sys_user或app_user表） |
| `username` | VARCHAR(50) | | 操作用户名（冗余存储，便于查询展示） |
| `user_from` | VARCHAR(10) | NOT NULL | 用户来源（sys/app） |
| `operation` | VARCHAR(100) | NOT NULL | 操作类型（CREATE/UPDATE/DELETE/SYNC/BACKUP/RESTORE等，见 OperationType 枚举） |
| `module` | VARCHAR(50) | NOT NULL | 操作模块（knowledge/ai_config/tools/statistics/data等） |
| `target_id` | BIGINT | | 操作目标ID（如知识源ID、文档ID等，便于追溯） |
| `target_type` | VARCHAR(50) | | 操作目标类型（如 KnowledgeSource/Document/AiConfig/Tool） |
| `content` | TEXT | | 操作内容详情（JSON格式，记录操作前后的变更内容） |
| `status` | VARCHAR(20) | DEFAULT 'SUCCESS' | 操作状态（SUCCESS/FAILED） |
| `error_msg` | VARCHAR(500) | | 失败原因（status=FAILED时记录） |
| `ip_address` | VARCHAR(50) | | 操作者IP地址 |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `create_time` | DATETIME | NOT NULL | 创建时间（精确到秒） |

**索引设计**：
- `idx_user_id`：`user_id`（查询用户操作记录）
- `idx_module`：`module`（按模块过滤）
- `idx_operation`：`operation`（按操作类型过滤）
- `idx_target`：`target_id, target_type`（按操作目标查询）
- `idx_status`：`status`（筛选失败操作）
- `idx_create_date`：`create_date`（范围查询，核心索引）
- `idx_create_time`：`create_time`（精确时间范围查询）

### 7.4.4 对话记录表（sys_ai_conversation）

**实体类**：`SysAiConversation`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiConversation`

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 对话ID（雪花算法） |
| `session_id` | VARCHAR(50) | NOT NULL | 会话ID（同一会话的多轮对话共享此ID） |
| `user_id` | BIGINT | NOT NULL | 用户ID（关联sys_user或app_user表） |
| `user_from` | VARCHAR(10) | NOT NULL | 用户来源（sys/app），用于区分B端/C端 |
| `question` | TEXT | NOT NULL | 用户提问内容 |
| `answer` | LONGTEXT | | AI回答内容（流式输出完成后持久化） |
| `images` | TEXT | | 图片URL列表（JSON数组格式，如 `["url1","url2"]`，图文对话时使用） |
| `model` | VARCHAR(50) | NOT NULL | 使用的模型名称（如 qwen-max、qwen-vl-max） |
| `temperature` | DECIMAL(3,2) | | 温度参数（实际使用的值，0.00-2.00） |
| `top_k` | INT | DEFAULT 5 | RAG检索数量（实际使用的值） |
| `response_time` | INT | | 响应时间（毫秒，从接收请求到完成回答） |
| `token_usage` | INT | | 本次对话消耗的Token数量（输入+输出） |
| `tool_usage` | TEXT | | 工具调用信息（JSON格式，记录调用了哪些工具及结果） |
| `sources` | TEXT | | 引用来源（JSON格式，记录RAG检索命中的文档列表） |
| `status` | VARCHAR(20) | DEFAULT 'SUCCESS' | 对话状态（SUCCESS/FAILED/TIMEOUT） |
| `error_msg` | VARCHAR(500) | | 失败原因（status=FAILED时记录） |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `create_time` | DATETIME | NOT NULL | 创建时间（精确到秒，用于趋势统计） |

**索引设计**：
- `idx_session_id`：`session_id`（查询会话历史）
- `idx_user_id`：`user_id`（查询用户对话记录）
- `idx_user_from`：`user_from`（按用户来源统计）
- `idx_model`：`model`（按模型统计调用情况）
- `idx_status`：`status`（筛选失败对话）
- `idx_create_date`：`create_date`（范围查询，核心索引）
- `idx_create_time`：`create_time`（精确时间范围查询，用于趋势图）

### 7.4.5 AI权限表（sys_ai_permission）

**实体类**：`SysAiPermission`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiPermission`

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 权限ID（雪花算法） |
| `user_id` | BIGINT | FOREIGN KEY | 用户ID（关联sys_user或app_user） |
| `user_from` | VARCHAR(10) | NOT NULL | 用户来源（sys/app） |
| `model_code` | VARCHAR(50) | NOT NULL | 模型编码（如qwen-plus、gpt-4o） |
| `enabled` | BOOLEAN | DEFAULT TRUE | 是否启用 |
| `quota` | INT | DEFAULT -1 | 调用配额（-1表示无限制） |
| `used_quota` | INT | DEFAULT 0 | 已使用配额 |
| `expire_date` | BIGINT | | 过期日期（格式：20260712，为空表示永久） |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `update_date` | BIGINT | NOT NULL | 更新日期（格式：20260712） |

**索引设计**：
- `idx_user_id`：`user_id`
- `idx_user_from`：`user_from`
- `idx_model_code`：`model_code`
- `idx_enabled`：`enabled`
- `uk_user_model`：`user_id, user_from, model_code`（唯一索引）

**权限控制逻辑**：
1. 用户请求AI对话时，先查询`sys_ai_permission`表
2. 如果用户没有对应模型的权限记录，或`enabled=false`，拒绝调用
3. 如果`quota > 0`且`used_quota >= quota`，拒绝调用
4. 如果`expire_date`不为空且已过期，拒绝调用

### 7.4.6 AI配置表（sys_ai_config）

**实体类**：`SysAiConfig`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiConfig`

**说明**：存储 AI 模型配置参数，支持运行时动态修改。配置变更后通过 Nacos 配置刷新或手动触发生效。

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 配置ID（雪花算法） |
| `config_key` | VARCHAR(100) | NOT NULL, UNIQUE | 配置项键名（如 default_model、temperature、max_tokens） |
| `config_value` | VARCHAR(500) | NOT NULL | 配置项值（字符串存储，使用时按类型转换） |
| `value_type` | VARCHAR(20) | NOT NULL | 值类型（STRING/INTEGER/DOUBLE/BOOLEAN） |
| `description` | VARCHAR(200) | | 配置说明 |
| `enabled` | TINYINT(1) | DEFAULT 1 | 是否启用 |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `update_date` | BIGINT | NOT NULL | 更新日期（格式：20260712） |

**索引设计**：
- `uk_config_key`：`config_key`（唯一索引，配置键不可重复）
- `idx_enabled`：`enabled`（筛选启用的配置）

**预置配置数据**：

| config_key | config_value | value_type | description |
|-----------|-------------|-----------|-------------|
| `default_model` | `qwen-max` | STRING | 默认文本模型 |
| `default_vision_model` | `qwen-vl-max` | STRING | 默认图文模型 |
| `temperature` | `0.7` | DOUBLE | 温度参数 |
| `max_tokens` | `4096` | INTEGER | 最大Token数 |
| `embedding_model` | `text-embedding-v1` | STRING | 向量嵌入模型 |
| `top_k` | `5` | INTEGER | RAG检索默认数量 |
| `enable_rag` | `true` | BOOLEAN | 是否启用RAG检索 |
| `enable_tools` | `true` | BOOLEAN | 是否启用Agent工具调用 |
| `max_file_size` | `10485760` | INTEGER | 文件读取最大大小（字节） |

### 7.4.7 工具配置表（sys_ai_tool_config）

**实体类**：`SysAiToolConfig`（继承 `BaseDO`）

**路径**：`com.zmbdp.chat.service.domain.entity.SysAiToolConfig`

**说明**：存储 Agent 工具的配置参数（如路径白名单、最大文件大小等），支持运行时动态修改。

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| `id` | BIGINT | PRIMARY KEY | 配置ID（雪花算法） |
| `tool_name` | VARCHAR(100) | NOT NULL, UNIQUE | 工具类名（如 ReadFileTool、SearchCodeTool） |
| `description` | VARCHAR(200) | | 工具描述 |
| `enabled` | TINYINT(1) | NOT NULL, DEFAULT 1 | 是否启用（1=启用，0=禁用） |
| `config_json` | TEXT | | 工具配置（JSON格式，如 `{"maxFileSize":10485760,"pathWhitelist":["/project/src"]}`） |
| `create_date` | BIGINT | NOT NULL | 创建日期（格式：20260712） |
| `update_date` | BIGINT | NOT NULL | 更新日期（格式：20260712） |

**索引设计**：
- `uk_tool_name`：`tool_name`（唯一索引，工具名不可重复）
- `idx_enabled`：`enabled`（筛选启用的工具）

**预置工具配置数据**：

| tool_name | enabled | config_json | description |
|-----------|---------|-------------|-------------|
| `ReadFileTool` | 1 | `{"maxFileSize":10485760,"pathWhitelist":["/path/to/project"]}` | 读取指定文件内容 |
| `SearchCodeTool` | 1 | `{"limit":10}` | 搜索代码中的类/方法 |
| `ListDirTool` | 1 | `{"maxDepth":3}` | 列出目录结构 |
| `SearchInFileTool` | 1 | `{"maxMatches":20,"contextLines":2}` | 在文件中搜索关键字 |
| `NacosConfigTool` | 1 | `{}` | 查询Nacos配置项 |
| `CompareConfigTool` | 1 | `{}` | 对比不同环境配置 |
| `PreDeployCheckTool` | 1 | `{}` | 部署前检查清单 |

---

## 7.5 schema.sql数据库初始化脚本

```sql
-- =====================================
-- AI 知识源表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_knowledge_source` (
    `id` BIGINT PRIMARY KEY COMMENT '知识源ID（雪花算法）',
    `name` VARCHAR(100) NOT NULL COMMENT '知识源名称',
    `path` VARCHAR(500) NOT NULL COMMENT '文件路径（知识源根目录绝对路径）',
    `type` VARCHAR(20) NOT NULL COMMENT '类型（doc/javadoc/config）',
    `enabled` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否启用（1=启用，0=禁用）',
    `chunk_size` INT DEFAULT 500 COMMENT '分块大小（字符数）',
    `chunk_overlap` INT DEFAULT 50 COMMENT '分块重叠大小（字符数）',
    `last_sync_date` BIGINT COMMENT '最后同步日期（格式：20260712）',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `update_date` BIGINT NOT NULL COMMENT '更新日期（格式：20260712）',
    UNIQUE INDEX `uk_name` (`name`),
    INDEX `idx_path` (`path`),
    INDEX `idx_type` (`type`),
    INDEX `idx_enabled` (`enabled`),
    INDEX `idx_last_sync_date` (`last_sync_date`),
    INDEX `idx_create_date` (`create_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI知识源表';

-- =====================================
-- AI 文档表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_document` (
    `id` BIGINT PRIMARY KEY COMMENT '文档ID（雪花算法）',
    `knowledge_source_id` BIGINT COMMENT '所属知识源ID',
    `title` VARCHAR(200) NOT NULL COMMENT '文档标题',
    `path` VARCHAR(500) NOT NULL COMMENT '文件路径',
    `content` LONGTEXT COMMENT '完整文档内容',
    `type` VARCHAR(20) NOT NULL COMMENT '类型（doc/javadoc/config）',
    `module` VARCHAR(100) COMMENT '所属模块',
    `category` VARCHAR(100) COMMENT '功能类别',
    `status` VARCHAR(20) DEFAULT 'ACTIVE' COMMENT '状态（ACTIVE/DELETED）',
    `version` INT DEFAULT 1 COMMENT '版本号',
    `hash` VARCHAR(64) COMMENT '文件内容哈希（SHA-256）',
    `chunk_count` INT DEFAULT 0 COMMENT '分块数量',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `update_date` BIGINT NOT NULL COMMENT '更新日期（格式：20260712）',
    UNIQUE INDEX `uk_path` (`path`),
    INDEX `idx_knowledge_source_id` (`knowledge_source_id`),
    INDEX `idx_type` (`type`),
    INDEX `idx_module` (`module`),
    INDEX `idx_category` (`category`),
    INDEX `idx_status` (`status`),
    INDEX `idx_create_date` (`create_date`),
    CONSTRAINT `fk_sys_ai_document_knowledge_source` FOREIGN KEY (`knowledge_source_id`) REFERENCES `sys_ai_knowledge_source`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI文档表';

-- =====================================
-- AI 操作日志表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_operation_log` (
    `id` BIGINT PRIMARY KEY COMMENT '日志ID（雪花算法）',
    `user_id` BIGINT NOT NULL COMMENT '操作用户ID',
    `username` VARCHAR(50) COMMENT '操作用户名',
    `user_from` VARCHAR(10) NOT NULL COMMENT '用户来源（sys/app）',
    `operation` VARCHAR(100) NOT NULL COMMENT '操作类型（CREATE/UPDATE/DELETE/SYNC等）',
    `module` VARCHAR(50) NOT NULL COMMENT '操作模块（knowledge/ai_config/tools等）',
    `target_id` BIGINT COMMENT '操作目标ID',
    `target_type` VARCHAR(50) COMMENT '操作目标类型',
    `content` TEXT COMMENT '操作内容详情（JSON格式）',
    `status` VARCHAR(20) DEFAULT 'SUCCESS' COMMENT '操作状态（SUCCESS/FAILED）',
    `error_msg` VARCHAR(500) COMMENT '失败原因',
    `ip_address` VARCHAR(50) COMMENT '操作者IP地址',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `create_time` DATETIME NOT NULL COMMENT '创建时间（精确到秒）',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_module` (`module`),
    INDEX `idx_operation` (`operation`),
    INDEX `idx_target` (`target_id`, `target_type`),
    INDEX `idx_status` (`status`),
    INDEX `idx_create_date` (`create_date`),
    INDEX `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI操作日志表';

-- =====================================
-- AI 对话记录表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_conversation` (
    `id` BIGINT PRIMARY KEY COMMENT '对话ID（雪花算法）',
    `session_id` VARCHAR(50) NOT NULL COMMENT '会话ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `user_from` VARCHAR(10) NOT NULL COMMENT '用户来源（sys/app）',
    `question` TEXT NOT NULL COMMENT '用户提问内容',
    `answer` LONGTEXT COMMENT 'AI回答内容',
    `images` TEXT COMMENT '图片URL列表（JSON数组）',
    `model` VARCHAR(50) NOT NULL COMMENT '使用的模型名称',
    `temperature` DECIMAL(3,2) COMMENT '温度参数',
    `top_k` INT DEFAULT 5 COMMENT 'RAG检索数量',
    `response_time` INT COMMENT '响应时间（毫秒）',
    `token_usage` INT COMMENT 'Token消耗数量',
    `tool_usage` TEXT COMMENT '工具调用信息（JSON格式）',
    `sources` TEXT COMMENT '引用来源（JSON格式）',
    `status` VARCHAR(20) DEFAULT 'SUCCESS' COMMENT '对话状态（SUCCESS/FAILED/TIMEOUT）',
    `error_msg` VARCHAR(500) COMMENT '失败原因',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `create_time` DATETIME NOT NULL COMMENT '创建时间（精确到秒）',
    INDEX `idx_session_id` (`session_id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_user_from` (`user_from`),
    INDEX `idx_model` (`model`),
    INDEX `idx_status` (`status`),
    INDEX `idx_create_date` (`create_date`),
    INDEX `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI对话记录表';

-- =====================================
-- AI 权限表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_permission` (
    `id` BIGINT PRIMARY KEY COMMENT '权限ID（雪花算法）',
    `user_id` BIGINT COMMENT '用户ID（关联sys_user或app_user表）',
    `user_from` VARCHAR(10) NOT NULL COMMENT '用户来源（sys/app）',
    `model_code` VARCHAR(50) NOT NULL COMMENT '模型编码（如qwen-plus、gpt-4o）',
    `enabled` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
    `quota` INT DEFAULT -1 COMMENT '调用配额（-1表示无限制）',
    `used_quota` INT DEFAULT 0 COMMENT '已使用配额',
    `expire_date` BIGINT COMMENT '过期日期（格式：20260712，为空表示永久）',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `update_date` BIGINT NOT NULL COMMENT '更新日期（格式：20260712）',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_user_from` (`user_from`),
    INDEX `idx_model_code` (`model_code`),
    INDEX `idx_enabled` (`enabled`),
    UNIQUE INDEX `uk_user_model` (`user_id`, `user_from`, `model_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI权限表';

-- =====================================
-- AI 配置表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_config` (
    `id` BIGINT PRIMARY KEY COMMENT '配置ID（雪花算法）',
    `config_key` VARCHAR(100) NOT NULL COMMENT '配置项键名',
    `config_value` VARCHAR(500) NOT NULL COMMENT '配置项值',
    `value_type` VARCHAR(20) NOT NULL COMMENT '值类型（STRING/INTEGER/DOUBLE/BOOLEAN）',
    `description` VARCHAR(200) COMMENT '配置说明',
    `enabled` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `update_date` BIGINT NOT NULL COMMENT '更新日期（格式：20260712）',
    UNIQUE INDEX `uk_config_key` (`config_key`),
    INDEX `idx_enabled` (`enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI配置表';

-- AI 配置预置数据
INSERT INTO `sys_ai_config` (`id`, `config_key`, `config_value`, `value_type`, `description`, `enabled`, `create_date`, `update_date`) VALUES
(1, 'default_model', 'qwen-max', 'STRING', '默认文本模型', 1, 20260712, 20260712),
(2, 'default_vision_model', 'qwen-vl-max', 'STRING', '默认图文模型', 1, 20260712, 20260712),
(3, 'temperature', '0.7', 'DOUBLE', '温度参数', 1, 20260712, 20260712),
(4, 'max_tokens', '4096', 'INTEGER', '最大Token数', 1, 20260712, 20260712),
(5, 'embedding_model', 'text-embedding-v1', 'STRING', '向量嵌入模型', 1, 20260712, 20260712),
(6, 'top_k', '5', 'INTEGER', 'RAG检索默认数量', 1, 20260712, 20260712),
(7, 'enable_rag', 'true', 'BOOLEAN', '是否启用RAG检索', 1, 20260712, 20260712),
(8, 'enable_tools', 'true', 'BOOLEAN', '是否启用Agent工具调用', 1, 20260712, 20260712),
(9, 'max_file_size', '10485760', 'INTEGER', '文件读取最大大小（字节）', 1, 20260712, 20260712)
ON DUPLICATE KEY UPDATE `config_key` = VALUES(`config_key`);

-- =====================================
-- AI 工具配置表
-- =====================================
CREATE TABLE IF NOT EXISTS `sys_ai_tool_config` (
    `id` BIGINT PRIMARY KEY COMMENT '配置ID（雪花算法）',
    `tool_name` VARCHAR(100) NOT NULL COMMENT '工具类名',
    `description` VARCHAR(200) COMMENT '工具描述',
    `enabled` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否启用（1=启用，0=禁用）',
    `config_json` TEXT COMMENT '工具配置（JSON格式）',
    `create_date` BIGINT NOT NULL COMMENT '创建日期（格式：20260712）',
    `update_date` BIGINT NOT NULL COMMENT '更新日期（格式：20260712）',
    UNIQUE INDEX `uk_tool_name` (`tool_name`),
    INDEX `idx_enabled` (`enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI工具配置表';

-- 工具配置预置数据
INSERT INTO `sys_ai_tool_config` (`id`, `tool_name`, `description`, `enabled`, `config_json`, `create_date`, `update_date`) VALUES
(1, 'ReadFileTool', '读取指定文件内容', 1, '{"maxFileSize":10485760,"pathWhitelist":["/path/to/project"]}', 20260712, 20260712),
(2, 'SearchCodeTool', '搜索代码中的类/方法', 1, '{"limit":10}', 20260712, 20260712),
(3, 'ListDirTool', '列出目录结构', 1, '{"maxDepth":3}', 20260712, 20260712),
(4, 'SearchInFileTool', '在文件中搜索关键字', 1, '{"maxMatches":20,"contextLines":2}', 20260712, 20260712),
(5, 'NacosConfigTool', '查询Nacos配置项', 1, '{}', 20260712, 20260712),
(6, 'CompareConfigTool', '对比不同环境配置', 1, '{}', 20260712, 20260712),
(7, 'PreDeployCheckTool', '部署前检查清单', 1, '{}', 20260712, 20260712)
ON DUPLICATE KEY UPDATE `tool_name` = VALUES(`tool_name`);

-- 管理员账号已在zmbdp-admin-service的sys_user表中创建，本服务无需重复初始化
-- 初始权限数据需在后台管理页面配置
```

---

## 7.6 错误码定义

### 7.6.1 错误码枚举（ResultCode）

**类名**：`ResultCode`

**路径**：`com.zmbdp.common.domain.domain.ResultCode`

**说明**：AI模块直接使用项目现有的 `ResultCode` 枚举，遵循6位编码规则（前3位为HTTP状态码）。

**现有通用错误码**：

| 错误码 | 错误信息 | 说明 |
|-------|---------|------|
| `200000` | 操作成功 | 请求成功 |
| `400000` | 无效的参数 | 参数校验失败 |
| `401000` | 令牌不能为空 | Token缺失 |
| `401001` | 令牌已过期或验证不正确 | Token无效 |
| `401002` | 令牌已过期 | Token过期 |
| `404000` | 服务未找到 | 服务不存在 |
| `404001` | url未找到 | 路径不存在 |
| `500000` | 服务繁忙请稍后重试 | 系统内部异常 |
| `500001` | 操作失败 | 业务操作失败 |

### 7.6.2 AI模块新增业务错误码

需在 `ResultCode` 枚举中新增以下AI模块专属错误码：

| 错误码 | 错误信息 | 说明 |
|-------|---------|------|
| `403000` | 无权限访问此资源 | 用户来源与接口不匹配（Gateway层） |
| `403001` | 无模型调用权限 | 未配置该模型的调用权限 |
| `403002` | 模型调用配额已用尽 | 权限配额使用完毕 |
| `403003` | 权限已过期 | 权限有效期已过 |
| `404002` | 知识源不存在 | 知识源查询失败 |
| `404003` | 文档不存在 | 文档查询失败 |
| `500022` | AI配置未设置 | API Key等配置缺失 |
| `500023` | AI服务连接失败 | 无法连接到AI模型服务 |
| `500024` | AI服务响应超时 | AI模型响应超时 |
| `500025` | 工具调用失败 | Agent工具执行出错 |
| `500026` | 路径不在白名单 | 文件读取路径不在允许范围 |
| `500027` | 文件类型不支持 | 上传文件类型不在允许列表 |
| `500028` | 文件超过大小限制 | 上传文件超过最大限制 |

**重要说明**：
1. AI模块新增错误码从 `500022` 开始（现有最大业务码为 `500021` Excel文件写入失败）。
2. 错误码 `400020`（请求过于频繁）已被现有代码占用（频控），AI模块限流复用此错误码。
3. **用户管理相关错误码（如登录失败、用户已存在、用户已禁用等）复用脚手架现有定义**，AI模块不重复定义。例如 `400007`（账号已停用，登录失败）。

**现有错误码占用情况**（已核实 `ResultCode.java`）：

| 码段 | 占用情况 | 说明 |
|-----|---------|------|
| `400000-400007` | 已占用 | 参数校验、验证码、用户停用等 |
| `400020` | 已占用 | 请求过于频繁（频控） |
| `400021-400099` | **未占用** | 可用，但AI模块暂不使用 |
| `401000-401004` | 已占用 | Token相关 |
| `403000-403099` | **未占用** | AI模块用于权限相关 |
| `404000-404001` | 已占用 | 服务未找到、URL未找到 |
| `404002-404099` | **未占用** | AI模块用于资源不存在 |
| `405000` | 已占用 | 请求方法不支持 |
| `500000-500021` | 已占用 | 系统异常、操作失败、短信、OSS、地图、邮件、Excel等 |
| `500022+` | **未占用** | AI模块业务错误码 |

**项目现有错误码参考**（仅列出与AI模块相关的部分）：

| 错误码 | 错误信息 | 说明 |
|-------|---------|------|
| `400007` | 账号已停用，登录失败 | 用户禁用（现有） |
| `400020` | 请求过于频繁，请稍后重试 | 频控（现有） |
| `500016` | Excel 导出失败 | Excel操作（现有） |
| `500017` | Excel 导入失败 | Excel操作（现有） |
| `500018` | Excel 格式错误 | Excel操作（现有） |
| `500019` | Excel 数据验证失败 | Excel操作（现有） |
| `500020` | Excel 文件读取失败 | Excel操作（现有） |
| `500021` | Excel 文件写入失败 | Excel操作（现有） |

### 7.6.3 统一错误响应格式

**参数校验失败响应**（`MethodArgumentNotValidException`，由 `GlobalExceptionHandler` 处理）：
```json
{
  "code": 400000,
  "errMsg": "提问内容不能为空",
  "data": null
}
```

**业务异常响应**（`ServiceException`）：
```json
{
  "code": 403001,
  "errMsg": "无模型调用权限",
  "data": null
}
```

**系统异常响应**（兜底处理）：
```json
{
  "code": 500000,
  "errMsg": "服务繁忙请稍后重试",
  "data": null
}
```

### 7.6.4 统一响应格式

**类名**：`Result<T>`

**路径**：`com.zmbdp.common.domain.domain.Result`

**通用响应结构**：

```java
public class Result<T> {
    private int code;
    private String errMsg;
    private T data;
    
    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> fail(int code, String errMsg) { ... }
}
```

**成功响应示例**：

```json
{
  "code": 200000,
  "errMsg": "操作成功",
  "data": {
    "id": 1,
    "name": "文档知识库",
    "path": "/path/to/docs"
  }
}
```

**分页响应格式**：

使用项目现有的 `BasePageVO<T>`：

```java
public class BasePageVO<T> {
    Integer totals;      // 查询结果总数
    Integer totalPages;  // 总页数
    List<T> list;        // 数据列表
}
```

```json
{
  "code": 200000,
  "errMsg": "操作成功",
  "data": {
    "list": [
      { "id": 1, "name": "文档1" },
      { "id": 2, "name": "文档2" }
    ],
    "totals": 100,
    "totalPages": 5
  }
}
```

**错误响应示例**：

```json
{
  "code": 400000,
  "errMsg": "无效的参数",
  "data": null
}
```

---

## 7.7 全局异常处理

### 7.7.1 GlobalExceptionHandler

**类名**：`GlobalExceptionHandler`

**职责**：全局异常处理，统一错误响应格式

**路径**：`com.zmbdp.common.security.handler.GlobalExceptionHandler`

**说明**：AI模块直接使用项目现有的 `GlobalExceptionHandler`，无需重新定义。该类使用 `@RestControllerAdvice` 注解，自动拦截所有控制器抛出的异常，统一转换为 `Result<T>` 格式返回。

**处理的异常类型**：

| 异常类型 | 处理方式 | 返回码 |
|---------|---------|-------|
| `MethodArgumentNotValidException` | 参数校验失败，返回具体字段错误 | 400000 |
| `ConstraintViolationException` | 参数约束违反 | 400000 |
| `HttpRequestMethodNotSupportedException` | 请求方式不支持 | 405000 |
| `MethodArgumentTypeMismatchException` | 参数类型不匹配 | 400006 |
| `NoResourceFoundException` | URL未找到 | 404001 |
| `ServiceException` | 业务异常，直接返回业务错误码 | 自定义 |
| `RuntimeException` | 运行时异常 | 500000 |
| `Exception` | 系统异常（兜底处理） | 500000 |

### 7.7.2 ServiceException

**类名**：`ServiceException`

**职责**：业务异常基类

**路径**：`com.zmbdp.common.domain.exception.ServiceException`

**使用方式**：

```java
throw new ServiceException(500023, "AI服务连接失败");
throw new ServiceException(ResultCode.INVALID_PARA.getCode(), "参数校验失败");
```

---

## 7.8 枚举定义

### 7.8.1 DocumentType（文档类型）

**类名**：`DocumentType`

**路径**：`com.zmbdp.chat.api.common.enums.DocumentType`

| 枚举值 | 说明 |
|-------|------|
| `DOC` | Markdown文档 |
| `JAVADOC` | JavaDoc文档 |
| `CONFIG` | 配置文件 |
| `CODE` | 源代码文件 |

### 7.8.2 DocumentStatus（文档状态）

**类名**：`DocumentStatus`

**路径**：`com.zmbdp.chat.api.common.enums.DocumentStatus`

| 枚举值 | 说明 |
|-------|------|
| `ACTIVE` | 活跃状态，可检索 |
| `DELETED` | 已删除，不可检索 |

### 7.8.3 ToolStatus（工具状态）

**类名**：`ToolStatus`

**路径**：`com.zmbdp.chat.api.common.enums.ToolStatus`

| 枚举值 | 说明 |
|-------|------|
| `ENABLED` | 已启用 |
| `DISABLED` | 已禁用 |

### 7.8.4 OperationType（操作类型）

**类名**：`OperationType`

**路径**：`com.zmbdp.chat.api.common.enums.OperationType`

| 枚举值 | 说明 |
|-------|------|
| `CREATE` | 创建 |
| `UPDATE` | 更新 |
| `DELETE` | 删除 |
| `QUERY` | 查询 |
| `SYNC` | 同步 |
| `LOGIN` | 登录 |

### 7.8.5 PermissionType（权限类型，预留枚举）

**类名**：`PermissionType`

**路径**：`com.zmbdp.chat.api.common.enums.PermissionType`

**说明**：**预留枚举，当前未使用**。脚手架未实现细粒度权限控制（无 sys_permission 表），此枚举为 v2.0 RBAC 扩展预留。

| 枚举值 | 说明 |
|-------|------|
| `MENU` | 菜单权限（预留） |
| `API` | 接口权限（预留） |

### 7.8.6 ModelType（模型类型）

**类名**：`ModelType`

**路径**：`com.zmbdp.chat.api.common.enums.ModelType`

| 枚举值 | 说明 | 支持能力 |
|-------|------|---------|
| `TEXT_ONLY` | 纯文本模型 | 仅文本对话 |
| `TEXT_AND_IMAGE` | 图文模型 | 文本+图片对话 |
| `EMBEDDING` | 嵌入模型 | 向量生成 |

### 7.8.7 ModelProvider（模型供应商）

**类名**：`ModelProvider`

**路径**：`com.zmbdp.chat.api.common.enums.ModelProvider`

| 枚举值 | 说明 |
|-------|------|
| `DASHSCOPE` | 通义千问（阿里） |
| `OPENAI` | OpenAI |
| `ZHIPU` | 智谱AI |
| `GEMINI` | Google Gemini |

### 7.8.8 ModelCapability（模型能力）

**类名**：`ModelCapability`

**路径**：`com.zmbdp.chat.api.common.enums.ModelCapability`

| 枚举值 | 说明 |
|-------|------|
| `TEXT` | 文本生成 |
| `IMAGE` | 图片理解 |
| `EMBEDDING` | 向量嵌入 |
| `FUNCTION_CALL` | 函数调用 |

---

## 7.9 定时任务配置

### 7.9.1 ScheduledConfig

**类名**：`ScheduledConfig`

**职责**：定时任务配置

**路径**：`com.zmbdp.chat.service.config.ScheduledConfig`

**配置内容**：
```java
@Configuration
@EnableScheduling
public class ScheduledConfig {
    @Bean
    public ThreadPoolTaskScheduler taskScheduler() {
        ThreadPoolTaskScheduler scheduler = new ThreadPoolTaskScheduler();
        scheduler.setPoolSize(5);
        scheduler.setThreadNamePrefix("scheduled-");
        scheduler.setWaitForTasksToCompleteOnShutdown(true);
        scheduler.setAwaitTerminationSeconds(60);
        return scheduler;
    }
}
```

### 7.9.2 KnowledgeSyncScheduler

**类名**：`KnowledgeSyncScheduler`

**职责**：知识同步定时任务

**路径**：`com.zmbdp.chat.service.scheduler.KnowledgeSyncScheduler`

**定时任务**：

| 任务 | 执行频率 | 说明 |
|-----|---------|------|
| `syncKnowledgeDaily()` | 每天凌晨2:00 | 自动同步所有知识源 |
| `cleanExpiredHistory()` | 每天凌晨3:00 | 清理过期的对话历史（超过30天） |
| `cleanExpiredLogs()` | 每天凌晨4:00 | 清理过期的操作日志（超过90天） |

**核心方法**：

```java
@Scheduled(cron = "0 0 2 * * ?")
public void syncKnowledgeDaily()
```
- **说明**：每天凌晨2点执行知识同步

```java
@Scheduled(cron = "0 0 3 * * ?")
public void cleanExpiredHistory()
```
- **说明**：每天凌晨3点清理过期对话历史

---

## 7.10 请求参数校验

### 7.10.1 校验注解使用规范

所有DTO字段必须添加校验注解，使用Jakarta Validation规范（JSR-380）。

**常用校验注解**：

| 注解 | 说明 | 示例 |
|-----|------|------|
| `@NotBlank` | 字符串不能为空 | `@NotBlank(message = "用户名不能为空")` |
| `@NotNull` | 对象不能为空 | `@NotNull(message = "知识源ID不能为空")` |
| `@Size` | 长度限制 | `@Size(min = 6, max = 20, message = "密码长度6-20位")` |
| `@Email` | 邮箱格式 | `@Email(message = "邮箱格式不正确")` |
| `@Min` | 最小值 | `@Min(value = 1, message = "页码最小为1")` |
| `@Max` | 最大值 | `@Max(value = 100, message = "每页数量最大为100")` |
| `@Pattern` | 正则校验 | `@Pattern(regexp = "^[a-zA-Z0-9_]+$", message = "用户名只能包含字母数字下划线")` |

### 7.10.2 DTO校验示例

**ChatRequest**：
```java
public class ChatRequest {
    @NotBlank(message = "提问内容不能为空")
    @Size(max = 10000, message = "提问内容不能超过10000字符")
    private String message;
    
    @Size(max = 50, message = "会话ID长度不能超过50字符")
    private String sessionId;
    
    @Min(value = 0, message = "温度参数最小为0")
    @Max(value = 2, message = "温度参数最大为2")
    private Double temperature;
}
```

**KnowledgeSourceReqDTO**：
```java
public class KnowledgeSourceReqDTO {
    @NotBlank(message = "知识源名称不能为空")
    @Size(max = 100, message = "知识源名称不能超过100字符")
    private String name;
    
    @NotBlank(message = "文件路径不能为空")
    @Size(max = 500, message = "文件路径不能超过500字符")
    private String path;
    
    @NotNull(message = "类型不能为空")
    @Pattern(regexp = "^(doc|javadoc|config)$", message = "类型只能是doc/javadoc/config")
    private String type;
}
```

**UserDTO**：
```java
public class UserDTO {
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 50, message = "用户名长度3-50字符")
    @Pattern(regexp = "^[a-zA-Z0-9_]+$", message = "用户名只能包含字母数字下划线")
    private String username;
    
    @Size(min = 6, max = 100, message = "密码长度6-100字符")
    private String password;
    
    @Email(message = "邮箱格式不正确")
    @Size(max = 100, message = "邮箱长度不能超过100字符")
    private String email;
    
    @NotNull(message = "是否启用不能为空")
    private Boolean enabled;
}
```

**AiConfigDTO**：
```java
public class AiConfigDTO {
    @Size(max = 100, message = "模型名称不能超过100字符")
    private String chatModel;
    
    @Min(value = 0, message = "温度参数最小为0")
    @Max(value = 2, message = "温度参数最大为2")
    private Double temperature;
    
    @Min(value = 1, message = "最大token数最小为1")
    @Max(value = 16384, message = "最大token数最大为16384")
    private Integer maxTokens;
    
    @Size(max = 100, message = "嵌入模型名称不能超过100字符")
    private String embeddingModel;
}
```

**KnowledgeSyncRequest**：
```java
public class KnowledgeSyncRequest {
    @NotBlank(message = "同步类型不能为空")
    @Pattern(regexp = "^(all|doc|javadoc|config)$", message = "同步类型只能是all/doc/javadoc/config")
    private String sourceType;
    
    @NotNull(message = "是否强制同步不能为空")
    private Boolean force;
}
```

---

## 7.11 核心流程图

```
┌──────────────────────────────────────────────────────────────────┐
│                        AI对话流程（C端）                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  用户请求                                                         │
│     ↓                                                            │
│  ChatController.chat()                                           │
│     ↓                                                            │
│  ChatService.chat()                                              │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 1: 获取对话历史（如果有sessionId）                    │  │
│  │  ChatMemoryService.getHistory(sessionId)                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 2: RAG检索上下文                                    │  │
│  │  embeddingModel.embed(userQuestion)                        │  │
│  │  vectorStore.search(vector, topK=5)                        │  │
│  │  → 获取相关文档列表                                          │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 3: 判断是否需要工具调用                                │  │
│  │  LLM分析问题和上下文                                        │  │
│  │  → 需要工具调用 → 选择工具 → 执行工具 → 获取结果            │  │
│  │  → 不需要工具调用 → 直接进入下一步                          │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 4: 构建Prompt                                       │  │
│  │  buildPrompt(history + context + userQuestion)             │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 5: 调用LLM生成回答                                   │  │
│  │  chatClient.call(prompt)                                   │  │
│  │  → 生成回答                                                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 6: 保存对话历史（如果有sessionId）                    │  │
│  │  ChatMemoryService.addMessages(sessionId, messages)        │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Step 7: 记录对话到数据库（用于统计）                      │  │
│  │  ConversationRepository.save(conversation)                 │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↓                                                            │
│  返回ChatResponse                                                │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

**文档版本**：v1.5  
**创建日期**：2026-07-12  
**最后更新**：2026-07-13  
**适用版本**：Scaffold AI Assistant v1.0